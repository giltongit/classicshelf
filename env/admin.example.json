{
  "_comment": "tool/seed_works.dart 전용. env/admin.json 으로 복사해 채울 것 (gitignore 대상). service_role 키는 RLS를 전면 우회하므로 앱이 읽는 dev.json 과 반드시 분리한다 — dev.json 은 --dart-define-from-file 로 앱 빌드에 주입된다.",
  "SUPABASE_URL": "",
  "SUPABASE_SERVICE_ROLE_KEY": ""
}
