# 클래식 음반 컬렉션 앱 — 아키텍처 v04

> **본 문서는 `아키텍처_v03.md`를 대체한다.** 이후 개발은 본 문서만 참조한다.
> 플랫폼: Flutter (iOS 우선 → Android) / Backend: Supabase / Local DB: Drift (SQLite)
> 개발 형태: 솔로 개발, 상업 출시 목표

> **v04의 성격**: v03까지가 "목표 사양"이었다면, v04는 **목표 사양 + 현재 구현 상태**를 함께 담는다.
> 데이터 계층과 코어 화면이 실제 구현되면서 확정된 결정들(클라이언트 UUID, sync_queue, 애그리게이트 저장,
> Album 단위 LWW, AlbumSummary 뷰 분리)을 반영하고, §19에 무엇이 구현되고 무엇이 스텁인지 명시한다.

---

## 0. 문서 위상

| 문서 | 상태 |
|---|---|
| `classical_features1.md` · `아키텍처_v1/v02/v03.md` | 폐기 (순차 흡수) |
| `FEATURES.md` · `공통_아키텍처.md` | **폐기** (커밋 7f2acc7에서 삭제) |
| `아키텍처_v04.md` | 현행 단일 기준 |

**v04의 핵심 변경 (v03 대비)**
- 식별자 전략 확정: **클라이언트 생성 UUID 단일 키** (localId↔supabaseId 이중 폐기)
- **sync_queue 테이블** 도입 (오프라인 큐를 컬럼이 아닌 별도 테이블로)
- **애그리게이트 저장 모델** 확정 (Album 통째 커밋 + replace-children)
- **Album 단위 LWW** 확정
- **AlbumSummary** 경량 뷰 분리 (목록 성능)
- Isar→Drift 전환 **구현 완료**, 도서 앱 코드 완전 폐기
- 코어 화면(목록·상세·홈) 클래식 전환 **구현 완료**

---

## 1. 제품 정의 (v03 계승, 변경 없음)

### 1-1. 철학

| 앱이 하는 것 | 앱이 하지 않는 것 |
|---|---|
| 소장·희망 음반 정리 | 음원 스트리밍 |
| AI 음악 해설 | 음반 판매·시세 정보 |
| 악보 탐색 | 팝·록 등 비클래식 장르 |
| 클래식 특화 검색 | YouTube API를 통한 인앱 재생 |

**핵심 가치**: 클래식 음반 수집과 감상 문화의 고차원화. 스트리밍 대체재가 아니다.

### 1-2. v1.0 범위

**코어**: 음반 등록(자동입력+수동입력) · 소장/희망 관리(음반+작품 단위) · 클래식 특화 계층 조회·검색
**보조**: AI 음악 해설 · 유튜브 특화 검색 · 컬렉션 통계 · 관리자 편성 큐레이션

### 1-3. 제외 및 이관

| 항목 | 판단 | 근거 |
|---|---|---|
| 커뮤니티 | v1.1 이후 | Apple 1.2 UGC 의무(EULA·필터링·신고·차단·24시간 대응)가 솔로 개발자에게 영구 부담 |
| 공연 일정 | KOPIS 연동 이후 | 관리자 수동 편성 지속 불가 |
| 악보 | 조건부 별도 트랙 | 법적 리스크 최대 (§13) |
| 음반 시세·가격 · 비클래식 | 영구 제외 | 철학 위배 |

---

## 2. 기술 스택

| 영역 | 선택 | 상태 |
|---|---|---|
| Frontend (앱) | Flutter (Dart) | ✅ |
| Frontend (웹 콘솔) | React + Supabase JS | 미착수 (대 3.5) |
| 상태관리 | Riverpod | ✅ |
| 라우팅 | go_router | ✅ |
| Backend | Supabase (PostgreSQL, Edge Functions, RLS, Storage) | 스키마 ✅ / Edge Functions 미착수 |
| Local DB | Drift 2.34.x + drift_flutter | ✅ 구현 완료 |
| 로컬 DB 암호화 | SQLCipher (`sqlcipher_flutter_libs`) | **미적용** (§19, 배선만 가능) |
| 로컬 검색 | SQLite FTS5 (trigram) | **미구현** — 현재 LIKE 폴백 (§6-3) |
| 결제 | RevenueCat | 미착수 (대 4) |
| AI | Claude API / Vision API | 미착수 (대 3) |
| 참조 데이터 시드 | Open Opus 덤프 | 미착수 (대 1-A 잔여) |
| 바코드 | `mobile_scanner` | 미착수 (대 2, 의존성만 존재) |
| 외부 이동 | `url_launcher` | 미착수 (대 2/§8) |
| 인증 | Supabase Auth (익명·Email·Google·Apple) | 익명 로그인 ✅ |
| 앱 식별자 | `com.giltech.classicshelf` | ✅ (구 도서 앱과 병존) |

**의존성 정리 노트**: 현재 pubspec에 미사용 패키지(mobile_scanner·csv·file_picker·url_launcher·intl·http·geolocator·google_sign_in 등)가 남아 있으나 **대부분 대 2 이후 재사용 예정**이라 제거하지 않는다. `google_sign_in`은 네이티브 설정 의존성이라 자동 제거 위험. pubspec 정리는 대 7(출시 준비)에서 최종 확정.

---

## 3. 데이터 모델 ★ (구현 완료, v04에서 대폭 확정)

### 3-0. 식별자 전략 (v04 확정) ★

**클라이언트 생성 UUID 단일 키.** 사용자 데이터 PK는 클라이언트가 생성한 UUID(text)이며, 로컬·원격이 동일 id를 쓴다.

- 오프라인에서 앨범 생성 시 자식(수록곡)이 **부모의 UUID를 즉시 참조** → 동기화 시 id 재매핑 불필요.
- 서버는 클라이언트 제공 id를 허용(`gen_random_uuid()`는 미지정 시 폴백).
- 도서 앱의 `int localId + nullable supabaseId` 이중 식별자를 **폐기**. 계층(Album→Composition→Movement) 구조에서 이중 식별자는 재매핑 복잡도가 계층 깊이에 비례해 부적합.
- 부수 효과: 표지 업로드가 생성 즉시 uuid 경로로 진행 → localId→supabaseId "promote 춤" 소멸.

### 3-1. 계층

```
Works (정규 작품 · 참조 데이터)
 ├─ 작곡가 · 작품명 · 작품번호 · 조성 · 장르 · 시대 · popular/recommended · source
 ├─ WorkMovements (표준 악장 구성)
 └─ WorkAliases (표기 변형)

Album (음반 · 애그리게이트 루트)
 ├─ 기본 연주자 (AlbumPerformers)
 └─ Composition (수록곡)
     ├─ → Works 참조 (nullable — 미매칭 허용)
     ├─ 디스크 번호 · 트랙 범위 · confidence
     ├─ 연주자 예외 (CompositionPerformers) → 없으면 앨범 기본값 상속
     └─ Movement (실제 수록 악장)

Wishlist (독립 애그리게이트 루트)
 ├─ type=album → Album 참조
 └─ type=work  → Works 참조

Commentaries (AI 해설 캐시, 공용)
SyncQueue (로컬 고유 — 오프라인 동기화 큐)
```

**총 11 테이블** (참조 4 + 사용자 6 + 큐 1). Supabase 마이그레이션 `20260725052147_init_classical_schema.sql`로 확정.

### 3-2. 애그리게이트 경계 (v04 확정) ★

- **Album이 애그리게이트 루트.** Composition·Movement·Performer는 그 내부. 저장·동기화의 커밋 단위는 Album 통째.
- **Wishlist는 독립 루트.** work 단위 희망은 앨범과 무관하게 존재.
- **Works는 참조 데이터** (애그리게이트 아님, 읽기 전용).

### 3-3. 연주자 상속 (§3-2 계승 + 구현 확정) ★

연주자를 Album이 아니라 **Composition에 귀속**. 저장은 "앨범 기본값(AlbumPerformers) + 곡별 예외(CompositionPerformers)"로 분리.

**상속 규칙은 도메인 메서드 `Composition.effectivePerformers(album)` 한 곳에 갇힌다:**
- 역할(role)별로 override가 있으면 그것, 없으면 앨범 기본값 상속.
- override가 아예 없으면(`performerOverrides == null`) 전부 상속.
- **null(상속)과 빈 리스트를 구분** — 조립 시점에 상속을 펼치지 않는다. 펼치면 재저장 시 앨범 기본값 변경이 곡에 전파 안 되는 버그 발생.

**상세 화면 표시 = 방식 2**: 앨범 헤더에 기본 연주자를 한 번(크림색), 곡별 override만 곡 아래(금색). `effectivePerformers`를 펼쳐 전부 표시하지 않는다 — 상속분/예외분을 색으로 구분해 "이 곡만 다르다"가 드러나게.

### 3-4. WorkMovements vs Movements

| | WorkMovements | Movements |
|---|---|---|
| 성격 | 작품의 **표준** 악장 | 이 음반에 **실제 수록된** 악장 |
| 소속 | Works (참조) | Composition (사용자) |

악장 자동 로드(§4-3)는 `Work.toMovements(uuidGen)`로 WorkMovements를 Movements로 복제. **대 2(MusicBrainz 연동) 이후 유효** — Open Opus 시드엔 악장이 없다.

### 3-5. 신뢰도 상태

모든 수록곡은 `confidence`(confirmed/unverified, §3-4). 미확인이어도 저장 허용, 목록·상세에 "확인 필요" 배지. 공용 캐시 반영은 확정값만. 검증 큐(§4-5)가 이 값으로 필터.

### 3-6. 캐시 vs 로컬 고유 상태

| 구분 | 대상 | 스키마 변경 시 |
|---|---|---|
| 캐시 | Works/WorkMovements/WorkAliases/Commentaries, Albums/Compositions/Movements/Performers/Wishlist | drop & 재동기화 |
| 로컬 고유 | **SyncQueue** (미전송 편집) | 정식 마이그레이션 필수 |

타입 매핑: `uuid`→TEXT, `timestamptz`/`date`→DateTime, `jsonb`→TEXT.

### 3-7. FK 강제 비대칭 (v04 확정) ★

- **로컬(Drift): FK PRAGMA off.** `.references()`는 문서화용. 오프라인 삽입 순서 제약을 피하기 위해 강제하지 않는다. 무결성의 source of truth는 Supabase.
- **서버(Supabase): FK cascade on.** 앨범 삭제 시 하위 자동 삭제.
- 결과: 로컬 삭제는 하위를 **명시적으로** 지워야 하고(cascade 없음), 서버 삭제는 albums 한 번으로 끝난다. 이 비대칭을 리포지토리가 처리.

### 3-8. 소유권 검증

`user_id`를 하위 테이블에도 중복 저장(RLS 단순화). 단 "내 user_id + 남의 부모" 조합을 막기 위해 **부모 소유권 검증 트리거**(`assert_parent_owner`)를 compositions/movements/performers에 `before insert or update`로 적용.

### 3-9. Works 참조 데이터 출처 (v03 계승)

3단계 하이브리드: Open Opus 덤프(시드, 악장 없음) → MusicBrainz Work(악장·별칭, 대 2) → 자체 축적(검증 큐→검수→공용). **현재 미착수** — 대 1-A 잔여 작업.

---

## 4. 음반 등록 (v03 계승, 구현은 대부분 미착수)

### 4-1. AlbumDraft 수렴 모델

```
📷 바코드 · 🔍 텍스트 · 📸 커버 · ✏️ 수동  →  AlbumDraft  →  확인·보정 화면(=수동 폼)  →  저장
```

**현재 상태**: 등록 폼 전체가 **스텁**(§19). 자동입력 4수단 모두 미구현. 2B-2에서 수동 폼부터 착수.

### 4-2~4-8 (v03 계승)

입력 수단 우선순위(바코드→뒷면 트랙리스트 OCR→텍스트→수동), 등록 폼 Step 1~4, 표 형식 자동 전환(6곡+), 일괄 촬영(촬영/검증 분리, 실패해도 이미지 보존), 검증 큐, 보류함(3개월 보관), CSV 가져오기·내보내기, 기기 전략(모바일 우선). **전부 v03 그대로, 구현 미착수.**

핵심 원칙 재확인: 뒷면 트랙리스트 OCR을 컴필레이션 1차 경로로. 앞면 커버는 보조. 일괄 촬영은 인식 실패해도 이미지를 보류함에 보존.

---

## 5. 메타데이터 파이프라인 (v03 계승, 미착수)

바코드/OCR/텍스트 → 자체 캐시 → MusicBrainz → OCR 폴백 → Discogs·국립중앙도서관·네이버 → AlbumDraft → Works 매칭 → 확정값만 공용 캐시.

Rate limit: MusicBrainz 1req/sec+User-Agent 필수(글로벌 병목 주의), Discogs 60/min, Open Opus 덤프 보관이라 런타임 호출 없음. **전부 대 2.**

---

## 6. 컬렉션 관리 ★ (조회·목록 구현 완료)

### 6-1. 목록 · 상세 (구현 완료)

- **목록**: `AlbumSummary` 경량 뷰 사용. 카드(커버·title·primaryComposer·format·releaseYear·수록곡수·확인필요/처분 배지). reactive(`albumSummariesProvider`, Drift watch). 카드 탭 → `/albums/:id`.
- **상세**: `Album` 애그리게이트 통째 조립. 3단 계층 표시(Album→Composition→Movement), 연주자 상속 방식 2, 삭제.
- **AlbumSummary vs Album**: 목록은 경량 뷰(하위 없음, 집계만), 상세는 애그리게이트 통째. book처럼 한 모델로 목록·상세·저장을 겸하지 않는다.

### 6-2. 소장 / 희망 (v03 계승, 조회만 구현)

희망 목록 음반 단위 + 작품 단위. 자동 해소 감지(등록 음반에 희망 작품 포함 시 알림). `getWishlist` 구현됨, UI 미착수. 처분·분실은 `disposedAt`(§6-2, HoldingStatus owned/disposed).

### 6-3. 검색 · 필터 (부분 구현)

- **결정: FTS5 trigram + 2글자 이하 LIKE 폴백** (v03 계승). **현재는 LIKE 폴백만 구현**, FTS5 가상 테이블 미생성(대 1-E). `// TODO: FTS5 trigram`.
- 필터 축: status/format/sort/composer/period/conductor/onlyNeedsVerification — **리포지토리 쿼리는 구현됨**. 단 **필터 UI(시트)는 스텁**, 정렬·검색만 화면 연결됨.
- 원어(라틴) 입력 경향 반영: trigram이 양쪽 문자 체계 처리. 작곡가 단위 한국어 별칭만 소량 선구축(§17). 초성 검색은 한국어 표기 필드에만.

### 6-4. 통계 (전면 미구현)

v03 계승(작곡가·시대·장르·연주자별). **도서 앱 통계 6화면은 폐기됨**(KDC 기반이라 클래식 대응 불가). 클래식 통계는 대 5에서 새로 작성.

---

## 7. AI 음악 해설 (v03 계승, 미착수)

Claude API → Edge Function 프록시. 캐시 키 `workId+language+version`. 무료 월 10회 서버 강제. 오프라인 열람만. **모델(`commentary.dart`)만 존재, 리포지토리·화면·Edge Function 전부 미구현**(대 3).

---

## 8. 유튜브 특화 검색 (v03 계승, 미착수)

`url_launcher` 외부 이동, API 미사용. 대 2/대 5.

---

## 9. 악보 (조건부 별도 트랙, v03 계승)

Mutopia(작품별 라이선스) / IMSLP(지역 저작권, v1은 외부 이동만). 대 6.

---

## 10. 오프라인 & 동기화 ★ (write·flush 구현, sync 미구현)

### 10-1. 오프라인 지원 (v03 계승)

등록된 정보·해설 열람 ✅, 작품 자동완성(로컬 Works 시드) — 시드 미착수. 신규 등록(큐 적재) ✅ 배관, 바코드·OCR·검색·해설생성은 온라인 전용.

### 10-2. 동기화 엔진 (구현 상태 명시) ★

- **write 경로 ✅**: `saveAlbum`이 애그리게이트를 Drift 트랜잭션으로 커밋(albums upsert + 하위 replace) 후, 온라인이면 Supabase 반영, 오프라인/실패면 sync_queue 적재.
- **sync_queue ✅**: `entityTable`/`entityId`/`operation`/`payload`(JSON 스냅샷)/`createdAt`, id autoIncrement.
  - 애그리게이트 저장 시 `albums`(insert) + `compositions`(replace_children) 2항목으로 적재. 개별 행 20개를 쌓지 않는다.
- **flush 경로 ✅**: entityId 그룹핑 → entityTable 순위 정렬(부모→자식) → 방향 분리(insert/update/replace_children 업서트 패스, delete 역순 패스).
  - **결정 1**: `replace_children`은 payload를 도메인으로 역직렬화 → 온라인 경로와 공유하는 `_replaceAlbumChildrenRemote` 재사용.
  - **결정 2**: 애그리게이트 단위 보존 — 그룹 중 하나라도 실패하면 그룹 전체 큐 보존, 전부 성공해야 삭제. 그룹 간 격리(try/catch).
  - 멱등성: 모든 op(upsert/delete-then-insert/PATCH/delete)가 멱등 → 부분 성공 후 재시도 안전.
  - DNS 실패 감지·재시도(`dnsFailures>0 && succeeded==0`), 오프라인 userId 공백 보정.
- **충돌 해결 = Album 단위 LWW** ★: `updatedAt`은 albums에만 존재(하위 없음). 하위 변경도 saveAlbum이 albums를 touch하므로 watch가 울림. **제약**: 하위만 직접 UPDATE하는 경로가 생기면 albums touch가 없어 watch가 안 울림 — 검증 큐 구현 시 주의.
- **sync 경로(원격→로컬) ❌**: `syncFromRemote`/`reconcileLocalOnlyToRemote` **미구현 스텁**(§19). pull-to-refresh는 임시 가드(스낵바).

### 10-3. 세 표현 경계 (v04 명시) ★

```
Drift Row (저장, syncState 없음·updatedAt 있음)
  ↕ 리포지토리 변환
Domain 모델 (순수, sync 무지)
  ↕ 리포지토리 변환
Supabase JSON (user_id 포함, 계층이 평면 테이블로 분해)
```

도메인 모델은 sync 관심사(syncState/updatedAt/cachedAt)를 갖지 않는다. user_id·부모 FK는 리포지토리가 덧붙인다.

---

## 11. 유료화 (v03 계승, 미착수)

무료: 등록 100건 · AI 월 10회 · 이미지 인식 평생 50앨범(사진 100장) · 기본 검색/통계 · 유튜브.
유료: 무제한 등록/AI/인식 · 악보 뷰어 · 고급 검색/통계 · CSV 대량 가져오기.
이미지 인식 평생 한도 + 차감 규칙(§11 v03). 결제 RevenueCat. **전부 대 4.**

---

## 12. 보안 요구사항 (v03 계승, 대부분 미구현)

| # | 요구사항 | 상태 |
|---|---|---|
| 1 | 등록 건수 한도 서버 강제 | ❌ 대 3 |
| 2 | AI 호출 한도 원자적 카운터 | ❌ 대 3 |
| 3 | Vision 호출 한도(평생 누적) | ❌ 대 2/3 |
| 4 | Edge Function JWT 검증 | ❌ 대 3 |
| 5 | RevenueCat webhook 서명 검증 | ❌ 대 4 |
| 6 | 전 테이블 default-deny RLS | ✅ 마이그레이션에 포함 |
| 7 | 캐시 오염 방지(검수 큐 경유) | ❌ 대 5 |
| 8 | 관리자 패널 키 관리·2FA | ❌ 대 5 |
| 9 | UGC 주입 방어 | N/A (커뮤니티 제외) |
| 10 | Storage 접근 제어 | ⚠️ 정책 확인 필요 |
| 11 | 로컬 DB 암호화(SQLCipher) | ❌ 배선만 가능(§19) |
| 12 | 웹 콘솔 권한 | ❌ 대 3.5 |
| 13 | 참조 데이터 쓰기 차단 | ✅ RLS SELECT-only |

**RLS·소유권 트리거는 마이그레이션에 구현 완료.** 서버측 한도 강제(1~5)는 Edge Function이 있어야 하므로 대 3 이후.

---

## 13. 법적 리스크 (v03 계승, 변경 없음)

IMSLP 지역 저작권 · Apple 4.2/1.2 · Mutopia 라이선스 · 커버 이미지 재배포(URL 참조) · Discogs 약관 · AI 해설 정확성 · 보류함 이미지 3개월 · MusicBrainz 필드별 라이선스(대 2 착수 전 확인). 상세 v03 §13.

---

## 14. 관리자 (4종, v03 계승, 미착수)

추천 큐레이션 · 공지 · 메타데이터 검수 큐 · 사용자 제재. 대 5.

---

## 15. 계정 & 컴플라이언스 (부분 구현)

- Supabase Auth: **익명 로그인 ✅**. Email/Google/Apple 미배선. `google_sign_in`은 네이티브 설정용으로 존재.
- 인앱 계정 삭제(Apple 요구) · 데이터 내보내기 · 처리방침 · 최소수집 — **미구현**(대 7).

---

## 16. 개발 단계 (진행 현황 반영) ★

```
대 0. 기반                    ✅ 완료 (프로젝트·Supabase·Drift·RLS)
대 1. 코어 컬렉션 루프         🔨 진행 중
  1-A 데이터 모델             ✅ (Works 시드 적재만 잔여)
  1-B 등록 폼                 ❌ 스텁 → 2B-2 다음 작업
  1-C 목록·상세·삭제          ✅
  1-D 소장/희망               ⚠️ 조회만(getWishlist), UI 미착수
  1-E 검색·필터               ⚠️ 정렬·검색 ✅, FTS5·필터UI 미착수
  1-F 동기화                  ⚠️ write·flush ✅, sync(원격→로컬) 미구현
  1-G 검증 큐·보류함           ❌
  1-H CSV                     ❌
대 2. 입력 자동화             ❌ (MusicBrainz·바코드·OCR·일괄촬영)
대 3. AI 해설 + 서버측 강제    ❌
대 3.5 웹 콘솔                ❌
대 4. 유료화                  ❌
대 5. 콘텐츠 + 관리자          ❌ (통계 재작성 포함)
대 6. 악보                    ❌
대 7. 출시 준비               ❌
대 8. 출시 후 → Android       ❌
```

**전환 작업 이력 (이번 세션)**: Isar→Drift 스키마 → 도메인 모델 7개 → 리포지토리(write/flush/read) → 프로바이더 재배선 → 도서 화면·서비스 삭제 → 코어 4화면 클래식 전환 → book 잔재 제거 → 실기 실행 확인 → 3커밋(7bff6ee/7f2acc7/5ff5237).

### 순서 원칙 (v03 계승 + 갱신)
1. 수동 입력을 바코드보다 먼저 — 폴백이 주 경로. (2B-2에서 수동 폼부터)
2. Works 시드를 등록 폼보다 먼저 — 자동완성이 입력 절감의 전제.
3. 서버측 강제(대 3)를 웹 콘솔·유료화보다 먼저.
4. 악보는 병렬 트랙.

---

## 17. 미결정 사항 (v03 계승 + 갱신)

| # | 항목 | 상태 |
|---|---|---|
| 1 | MusicBrainz 필드별 라이선스 | 대 2 착수 전 필수 |
| 2 | Vision 실단가 기반 한도 검증 | 대 2 착수 전 |
| 3 | Open Opus 덤프 갱신 주기 | 대 1-A |
| 4 | 로컬 Works 세트 범위(전체 vs 경량) | 대 1-A |
| 5 | CSV 스키마(Discogs 매핑) | 대 1-H |
| 6 | 웹 콘솔 인증 방식 | 대 3.5 |
| 7 | 작품 단위 희망 추천 연계 | v1.1 이후 |
| 8 | **SQLCipher 실적용 여부·성능 실측** | 배선 가능하나 미적용 (§19) |
| 9 | **하위 직접 수정 경로의 watch 갱신** | 검증 큐 구현 시 (§10-2 제약) |
| 10 | **.gitignore CP949 인코딩 정리** | 저低우선, 언젠가 UTF-8 변환 |
| 11 | **위시 자동 해소 감지** | 앨범 등록 시 매칭되는 위시 항목을 감지·해소하는 로직이 없음. 대 1-D에서 제외 |
| 12 | **위시 → album/work 확정 연결** | 지금은 composer/title 자유 텍스트만. album_id/work_id를 잇는 UI는 Works 시드(대 1-A) 이후, 11번과 묶어 처리 |

---

## 18. 현재 코드 구조 (v04 신규 — 실제 파일 지도)

```
lib/
├── models/                          도메인 모델 (순수, sync 무지)
│   ├── album.dart                   Album 애그리게이트(+Composition/Movement/Performer/enum)
│   ├── album_summary.dart           목록 경량 뷰
│   ├── work.dart                    Work+WorkMovement+WorkAlias (미배선 — 대 2)
│   ├── wishlist_entry.dart          WishItem+WishType
│   ├── commentary.dart              AI 해설 (미배선 — 대 3)
│   ├── album_filter.dart            필터+정렬
│   └── model_utils.dart             날짜 변환 공용
├── database/
│   └── app_database.dart(.g)        Drift 11테이블 미러
├── repositories/
│   ├── collection_repository.dart   인터페이스
│   └── collection_repository_impl.dart  write✅/flush✅/read✅/sync❌
├── providers/
│   ├── providers.dart               collectionRepository·albumSummaries·allAlbumSummaries·albumDetail·albumFilter·wishlist
│   ├── sync_queue_flusher.dart      앱시작·복귀 flush(promote 제거됨)
│   └── cover_upload_provider.dart   표지 업로드(promote 제거됨)
├── screens/
│   ├── home_screen.dart             ✅ allAlbumSummaries
│   ├── book_list_screen.dart        ✅ AlbumListScreen (파일명 유지)
│   ├── book_detail_screen.dart      ✅ AlbumDetailScreen
│   ├── add_book_screen.dart         ❌ 스텁(71줄) — 2B-2
│   ├── wishlist_screen.dart         ✅ WishlistScreen (대 1-D)
│   ├── main_scaffold.dart           탭 4개(홈·서가·희망·설정)
│   └── settings_screen.dart         book 의존 무력화
├── services/
│   └── cover_photo_service.dart     미배선(2B-2 커버 촬영 예정)
└── router/app_router.dart           /home·/·/wishlist·/settings·/add·/albums/:id

supabase/migrations/20260725052147_init_classical_schema.sql   11테이블+RLS+트리거
supabase/migrations/20260804120000_wishlist_free_text.sql      wishlist composer/title + CHECK 완화
```

**클래스명 정리**: `BookListScreen`→`AlbumListScreen`, `BookDetailScreen`→`AlbumDetailScreen` (파일명은 라우팅 최소 수정 위해 유지).

---

## 19. 구현 상태 요약 (v04 신규) ★

### ✅ 구현·검증 완료
- Drift 11테이블 스키마 + Supabase 마이그레이션(RLS·소유권 트리거·FK cascade)
- 도메인 모델 7개 (애그리게이트·상속·세 표현 경계)
- 리포지토리 **write**(애그리게이트 저장+큐 적재), **flush**(그룹핑·방향분리·LWW·멱등), **read**(상속 조립·N+1 회피·필터 쿼리)
- 프로바이더 재배선 (reactive 목록)
- 코어 화면: 목록·상세·홈 (클래식 전환)
- 도서 앱 코드 완전 폐기
- 실기(SM F731N) 부팅·화면전환·정렬 확인, `flutter analyze` 에러 0
- 3커밋 완료

### ❌ 미구현 (스텁 또는 미착수)
| 항목 | 위치 | 예정 |
|---|---|---|
| 등록 폼 | add_book 스텁(71줄) | **2B-2 (다음)** |
| syncFromRemote / reconcile | 리포지토리 스텁 | read/sync 경로 |
| Work 조인 표시 | 상세 `// TODO` | 대 2 |
| FTS5 trigram | LIKE 폴백 중 | 대 1-E |
| 필터 시트 UI | 스텁(스낵바) | 대 1-E |
| 소장/희망 UI | getWishlist만 | 대 1-D |
| Works 시드 적재 | 미착수 | 대 1-A |
| 자동입력(바코드·OCR·검색) | 미착수 | 대 2 |
| AI 해설 · Edge Function | 모델만 | 대 3 |
| 서버측 한도 강제 | 미착수 | 대 3 |
| SQLCipher 실적용 | 미적용 | 미결정(§17-8) |
| 통계 (클래식) | 폐기 후 미작성 | 대 5 |

### ⚠️ 배선 주의
- pull-to-refresh: syncFromRemote 미구현 → 임시 가드(스낵바). 구현 후 복원.
- watch 갱신: 하위 직접 UPDATE 경로 생기면 albums touch 없어 목록 미갱신 (§10-2).
- pubspec 미사용 의존성: 대 2 이후 재사용 예정이라 유지.

---

## 20. 변경 이력

**v04 (본 문서)** — `아키텍처_v03.md` 대체. 데이터 계층·코어 화면 구현 반영.
- 식별자 전략 확정: 클라이언트 UUID 단일 키 (§3-0)
- sync_queue 테이블 + 애그리게이트 저장 + Album 단위 LWW (§10-2)
- 애그리게이트 경계, 세 표현 경계, FK 비대칭, 소유권 트리거 명시 (§3-2,3-7,3-8,10-3)
- AlbumSummary 경량 뷰 분리 (§6-1)
- 연주자 상속 구현·방식 2 표시 확정 (§3-3)
- §16 개발 단계에 진행 현황 반영, §18 파일 지도·§19 구현 상태 신규
- 도서 앱 완전 폐기, 3커밋 기록

**v03** — Works 참조 데이터 3단 하이브리드, 원어 입력 검색 조정, WorkMovements 분리.
**v02** — Works 엔티티 신설, 작품 단위 희망, 미결정 6건 확정.
**v1** — Isar→Drift, 커뮤니티·공연 제외, 연주자 Composition 귀속, AlbumDraft 수렴, 신뢰도 상태, 보안 명문화.
