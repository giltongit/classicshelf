# 내서재 (mylibrary) — 공통 아키텍처 (개정본)

> 이 문서는 기존 `Bookshelf` 단일 앱 코드(FEATURES.md 스냅샷)를 **Supabase 기반 · 로컬 우선 · 내서재 멀티변형** 목표 아키텍처로 옮기기 위한 마이그레이션 기준 문서다.
> Claude Code 세션은 이 문서를 canonical source로 삼는다. 결정이 바뀌면 이 문서를 먼저 갱신하고 커밋한다.

---

## 0. 이번 개정에서 확정된 결정 요약

| # | 결정 | 영향 |
|---|---|---|
| 1 | 로그인 추가 — Supabase **Anonymous Auth** 기본, Google/Apple 선택적 연결(linkIdentity) | 익명 세션으로 마찰 없이 시작, 후일 계정 연결 시 `user_id` 유지 |
| 2 | Google Sheets/Apps Script 폐기 → **Supabase(PostgreSQL)** | `sheets_service.dart`, `Code.gs`, 302 추적, text/plain 우회 전부 제거 |
| 3 | Google Drive 표지 업로드 폐기 → **Supabase Storage** | base64→Apps Script→Drive→lh3→setSharing 사슬 제거 |
| 4 | 로컬 DB **sqflite → Drift** | 오프라인 유지, Supabase를 source of truth로 하는 3계층 동기화 |
| 5 | 상태관리 **provider → flutter_riverpod** | 목표 스택 정렬 |
| 6 | **공공도서관 자료 검색 + 위치 안내** 기능을 **코어에 편입** (신규) | 한국 이용자 공공도서관 접근성 = 앱 철학의 일부 |
| 7 | **영어 iOS 변형은 이번 범위에서 분리** (별도 제품 트랙) | 변형 3종(일반/시니어/아동)으로 축소 |
| 8 | iOS는 "미뤄둔 숙제" — 한국인용 앱이라도 추후 동일 3변형에 추가 | 코어는 지금부터 순수 Dart로 격리 유지 |
| 9 | 세션 시작 시 `supabaseId` 기준 로컬→원격 reconcile 자동 실행 (`reconcileLocalOnlyToRemote`) | 큐 수동 정리 중 원격 row 유실 방어. `SyncQueueFlusher.build()`에서 flush 직후 fire-and-forget. 멱등 — 원격 존재 시 스킵, `23505 unique_violation` 무시 |
| 10 | 5c-2c LWW(Last-Write-Wins) 충돌 해소는 `linkIdentity` 도입까지 보류 | `updated_at` 컬럼으로 충돌 시점 파악은 준비됨. 멀티 기기 본격 지원(소셜 계정 연결) 시점에 구현 |
| 11 | 오프라인에서 추가된 책의 cover는 `supabaseId` 확보 후에만 업로드·교체. `flushSyncQueue()`(book insert → supabaseId 발급) 완료 직후 `promoteLocalCover`가 `localId→supabaseId` 경로로 cover 업로드 + cover_url 원격 반영 | `CoverUploadNotifier`의 독립 connectivity 구독 제거 — cover flush는 항상 sync_queue flush 완료 후 순서 실행(race 방지). **불변: cover 업로드·cover_url 교체는 supabaseId 없이 하지 않는다** |
| 12 | 바코드 스캔 화면(`scan_screen`)을 **명시적 상태머신**(`_Phase` 8상태)으로 구현. 카메라 lifecycle은 `mobile_scanner` autoStart에 위임(수동 start/stop 미사용), `detectionSpeed.normal` + 앱레벨 dedup(`_lastProcessedIsbn`)으로 중복 차단 | 무한 재검색 루프·시스템 back 무반응 회귀 차단. **불변: dedup 락 해제는 "다시 스캔" 단일 지점에서만**. 상세 §8 |
| 13 | `covers` 버킷 **public 확정** — 표지는 비민감 정보, Storage RLS 미적용. 행 보안은 `books` RLS 전담 | Storage 정책 단순화, signed URL 불필요. 상세 §7 |
| 14 | 책 삭제 시 Storage 표지 파일 정리(best-effort, 즉시·큐 양경로). 외부URL·null 제외, 확장자 보존 | 행 삭제와 표지 정리 분리로 인한 고아 파일 방지. 복귀 직후 flush 실패는 재시도로 치유 |
| 15 | **모델 확장** — `status`에 `rental` 추가, 신규 필드 `is_read` / `medium` / `language` / `call_number` | `is_read`는 안티라이브러리 통계용, UI에서 조용하게 처리(미독을 결핍으로 표시 안 함). `medium` 기본값 `'paper'`. `language` 자동 감지(C-3 이후). 책장 위치·청구기호 자동완성(3회 이상 입력 시) — C-3 확정 |
| 16 | 집계 탭 폐기. 초성 그룹핑·서가 위치는 검색/필터로 이동, 저자는 통계로 흡수 | 탭 구조 단순화 |
| 17 | 탭 구조 확정 — 홈/서가/통계/설정 (4개) | 홈 탭 신규 추가 |
| 18 | UI 용어 확정 — "나의 도서관"(화면 제목·문구), "서가"(탭 라벨·탐색 맥락) | antilibrary = library 철학적 연결. "서재"는 전면 폐기 |
| 19 | 홈 화면 신규 — 배경 이미지 3슬롯(앱 시작마다 랜덤) + 도서관 명칭 사용자 입력 + 오늘의 책 + 기다린 지 오래된 책 + 나의 도서관 요약 | 앱 철학의 첫 인상 |
| 20 | profiles 테이블 확장 — tracking_started_at DATE + library_name TEXT | 월별 추이 추적 시작점 + 도서관 명칭 저장 |
| 21 | books 테이블 확장 — acquired_at DATE (책 만난 날, 선택적) | effectiveDate = acquired_at ?? created_at 패턴으로 월별 추이 집계 |
| 22 | 통계 화면 드릴다운 구조 확정 — 통계 홈 + 상세 4개 (/stats/year /stats/author /stats/monthly /stats/wishlist) | 스크롤 과다 방지 |
| 23 | 장르 정규화 — 별도 독립 과제로 분리. 네이버/구글 분류 체계 분석 + 1/2수준 설계 필요. 통계 화면 장르 섹션은 완료 후 추가 | 데이터 품질 미확보 상태에서 통계 구현 금지 |
| 24 | `priority_read` Drift 로컬 전용 확정 — Supabase 동기화 보류 | 토글 빈도 높음(도서관/서점 현장 사용), 멀티 기기 보류 단계에서 동기화 실익 없음. linkIdentity 도입 시 재검토. Supabase books 테이블 컬럼은 존재하므로 코드 변경만으로 추후 동기화 가능 |
| 25 | 서가 화면 필터/정렬 확정 — 바텀 시트 패널 | 상세 §15 |
| 26 | Google 계정 연결 방식 확정 — `linkIdentity(OAuthProvider.google)` 브라우저 redirect | 익명 user_id 유지, nonce 불필요. 상세 §16 |
| 27 | 장르 정규화 — KDC 런타임 파생으로 확정, 신규 컬럼/Drift migration 없음. 기존 genre 값은 전부 삭제, genre 필드는 사용자 자유 태그로 전환 | 네이버 API에 category 필드 없음이 실측으로 확인되어 원안 폐기. 상세 §24 |
| 28 | disposed(처분) 상태 — status는 'owned' 유지, `disposedAt`(nullable) 필드로 orthogonal하게 이력만 기록 (A안). 서가·통계 기본 제외, 필터 켜면 처분된 책만 배타적으로 노출. Drift v7. 실기기 검증 완료 | 소장 여부 판정은 `Book.isActiveOwned` getter로 일원화. 상세 §25 |
| 29 | 장르 구조화 입력 — KDC 대분류/중분류 드롭다운으로 `kdc`에 구조화 저장(미리 선택 + 변경 시만 재작성, 정밀 코드 보존). genre 컬럼은 "내 분류"(장르 종속 복수 태그, 쉼표 구분)로 재규정, 상세화면 인라인 칩. 스키마 변경 없음 | §24의 진화 — 미분류 90권 수동 입력 필수. 통계·필터 정합 유지. 상세 §26 |

### 작업 순서 (의존성 기준)
```
② Supabase 프로젝트 + 스키마/RLS
  → ① 인증 (auth.uid()가 있어야 RLS 성립)
  → ③ Storage (인증 의존)
  → 단일 앱을 Supabase 위에서 안정화 (Android, 일반 변형)
  → ④ 코어 추출 (mylibrary_core) + 변형 분기
  → (별도 트랙) iOS 플랫폼 추가 / 영어 변형
```
> ④를 먼저 하면 아직 흔들리는 데이터 계층을 패키지 경계로 굳혀 버리는 실수가 생긴다. 데이터 계층 안정화가 코어 추출의 선행 조건이다.

---

## 1. 목표 스택

| 항목 | 결정 |
|---|---|
| 백엔드 | Supabase (PostgreSQL + Storage + Anonymous Auth) |
| 인증 | Supabase Anonymous Auth + Google/Apple linkIdentity |
| 로컬 DB | **Drift** (로컬 캐시, Supabase=source of truth) |
| 상태관리 | flutter_riverpod |
| 아키텍처 | Clean Architecture, Feature-First |
| 모델 | freezed (immutable) |
| 네트워크 | dio |
| 라우팅 | go_router |
| 기타 | mobile_scanner ^7.2.0, speech_to_text, flutter_image_compress |
| 결제 | RevenueCat (구독 ~3,000원/월) |

### 3계층 데이터 흐름
```
UI (riverpod) ──▶ Repository (코어)
                     ├─ Drift (로컬 캐시 / 오프라인 읽기)
                     └─ Supabase (source of truth)
                          ▲
                          └─ 오프라인 동기화 큐 (온라인 복귀 시 flush)
```

---

## 2. 변형 · 플랫폼 매트릭스

### 이번 개발 범위
| 변형 | Android (now) | iOS (later) |
|---|---|---|
| 일반 (general) | ✅ | ⏳ 숙제 |
| 시니어 (senior) | ✅ | ⏳ 숙제 |
| 아동 (children) | ✅ | ⏳ 숙제 |

- **영어 iOS**: 이 매트릭스 밖의 **별도 제품 트랙**. 이번 범위에 포함하지 않는다.
- iOS는 "안 함"이 아니라 "지금 안 하지만 반드시 할 것". 동일 3변형을 코어 재사용으로 추가한다.

### iOS 트랙에서 처리할 숙제 (FEATURES.md 17장 + 위치 기능 추가분)
- `Info.plist`: 카메라 / 사진 권한 항목
- `Info.plist`: `NSLocationWhenInUseUsageDescription` (위치 기능 코어 편입으로 추가)
- CocoaPods 설정
- **Apple 로그인 추가** — 소셜 로그인 제공 시 Apple 로그인은 심사 의무. iOS 출시 시점에 반드시 포함. (Android 단독 단계에서는 후순위)

---

## 3. 코어 / 변형 책임 분할

기준: **"오디언스가 달라도 똑같은가"** → 같으면 코어, UI/게이팅이면 변형.

### `mylibrary_core` (순수 Dart, 플랫폼 의존 금지)
- **모델**: `Book`, `BookSearchResult`, `Library`, `LoanAvailability` (freezed)
- **데이터 계층**: Supabase 리포지토리 + Drift 로컬 캐시 + 오프라인 동기화 큐
- **인증 서비스**: 익명 세션, Google/Apple linkIdentity 흐름
- **도서 검색**: 네이버 → 구글 북스 폴백
- **공공도서관**: `LibraryRepository` 인터페이스 + 한국 구현체 (§5)
- **위치/거리**: GPS 좌표 획득 추상화, 거리 계산, 가까운 순 정렬
- **표지**: 촬영/리사이즈 로직 (FEATURES [COVER][1][2] 단계), Storage 업로드
- **바코드**: ISBN 파싱·컨트롤러 로직 (스캔 화면 상태머신은 변형 앱 UI, 컨트롤러·검색은 코어 — §8)
- **유틸**: `sort_utils`, `cover_color`, `ReadingStatus`/`SyncState` 등 enum

> ⚠️ **권한 선언은 코어에 못 넣는다.** 코어는 순수 Dart라 Android `Manifest`·iOS `Info.plist`가 없다. 로직은 코어, 권한 선언은 변형 앱마다 각자의 android/ios 폴더에.

### 변형 앱 (general / senior / children)
- `screens/`, `widgets/` — UI
- 테마(팔레트·타이포), `main.dart`, 라우터
- **기능 게이팅** (§6)
- 플랫폼 분기 — 권한 선언, 파일 경로, 카메라/스캐너 설정

### 모노레포 vs 멀티레포 (결정 필요)
- 기존 방향: 변형별 별도 Git 저장소 + 코어를 git/path 의존성으로
- 변형 3개로 늘면 코어 버전 동기화가 번거로워짐 → **melos 모노레포**(코어 1 + 변형 N)가 버전 핀 고통을 줄인다
- **저장소를 쪼개기 전에 둘 중 무엇으로 갈지 명시적으로 결정할 것** (현재 미확정)

---

## 4. Supabase 스키마 / RLS

### 스키마 변경 이력

| 버전 | 변경 내용 |
|---|---|
| Drift v1-v3 | 기본 books 테이블 + sync_queue |
| Drift v4 | is_read, medium, language, call_number, status rental 추가 |
| Drift v5 | books.acquired_at DATE 추가 |
| Drift v6 | books.kdc/ddc/lc TEXT 추가 |
| Drift v7 | books.disposedAt DateTime 추가 (§25) |
| — | profiles 테이블 신규 (tracking_started_at) |
| — | profiles.library_name TEXT 컬럼 추가 |

### Supabase profiles 테이블 (신규)

```sql
CREATE TABLE IF NOT EXISTS public.profiles (
  user_id uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  tracking_started_at DATE,
  library_name TEXT
);
ALTER TABLE public.profiles ENABLE ROW LEVEL SECURITY;
CREATE POLICY "select_own" ON public.profiles FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "insert_own" ON public.profiles FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "update_own" ON public.profiles FOR UPDATE
  USING (auth.uid() = user_id)
  WITH CHECK (auth.uid() = user_id);
CREATE POLICY "delete_own" ON public.profiles FOR DELETE USING (auth.uid() = user_id);
```

**library_name 컬럼 추가 (결정 #20)**

```sql
ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS library_name TEXT;
```

### acquired_at 컬럼 추가

```sql
ALTER TABLE books ADD COLUMN IF NOT EXISTS acquired_at DATE;
```

- 의미: 사용자가 해당 책을 만난(취득한) 날짜. add/edit 화면에서 선택적 입력.
- 통계 섹션 6(월별 책과의 만남)에서 `effectiveDate = acquired_at ?? created_at`로 사용.

```sql
create table public.books (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  title text not null,
  author text not null,
  isbn text,
  cover_url text,
  description text,
  status text not null default 'owned' check (status in ('owned','wishlist','rental')),
  review text,
  page_count int,
  year text,
  genre text,
  publisher text,
  location text,
  priority_read boolean not null default false,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  is_read boolean not null default false,
  medium text not null default 'paper' check (medium in ('paper','ebook','audio')),
  language text,
  call_number text
);

alter table public.books enable row level security;

-- ⚠️ SELECT/INSERT/UPDATE/DELETE 네 정책을 반드시 각각 정의. 하나라도 빠지면 사고.
create policy "select_own" on public.books for select using (auth.uid() = user_id);
create policy "insert_own" on public.books for insert with check (auth.uid() = user_id);
create policy "update_own" on public.books for update using (auth.uid() = user_id) with check (auth.uid() = user_id);
create policy "delete_own" on public.books for delete using (auth.uid() = user_id);
```

- 기존 시트의 `upsert/delete/list/stats` 4개 액션 → 위 4개 정책 + Supabase 클라이언트 호출로 대체
- `priority_read`는 시트엔 컬럼이 없어 로컬 전용이었으나, 이제 동기화 가능
- **C-3 신규 컬럼** — `is_read`, `medium`, `language`, `call_number`, `status` 에 `rental` 추가: Drift v4 마이그레이션 완료, Supabase 콘솔에서 ALTER TABLE로 동기 반영 필요

---

## 5. 공공도서관 — `LibraryRepository` (코어)

영어 변형을 뺐으므로 이 추상화의 동기는 **"다국가 대응"이 아니라 "한국 내 복수 도서관 데이터 소스 대응"**이다. 도서관정보나루·KOLIS-NET이라는 서로 다른 소스를 다루고, 한쪽 API가 죽었을 때 폴백하거나 소스를 교체할 여지를 남긴다 (네이버→구글 폴백과 같은 논리).

```dart
// mylibrary_core
abstract interface class LibraryRepository {
  /// 좌표 기준 근처 도서관 (거리순 정렬은 코어 위치 로직에서 처리)
  Future<List<Library>> findNearbyLibraries({
    required double lat,
    required double lng,
    double radiusKm = 5,
  });

  /// 특정 도서관의 해당 ISBN 소장/대출 가능 여부
  Future<LoanAvailability> checkAvailability({
    required String libraryCode,
    required String isbn,
  });
}

// 한국용 구현체: 도서관정보나루 + KOLIS-NET (폴백/병합)
class KoreaLibraryRepository implements LibraryRepository { /* ... */ }
```

- 위치 권한 선언(`ACCESS_FINE/COARSE_LOCATION`, iOS `NSLocationWhenInUseUsageDescription`)은 §3 원칙대로 **변형 앱**에.
- 기능을 끄는 변형/플랫폼은 권한도 빼서 불필요한 위치 권한 요구를 막는다.

---

## 6. 변형별 권한 · 게이팅 매트릭스

| 항목 | 일반 | 시니어 | 아동 |
|---|---|---|---|
| 공공도서관·위치 검색 | ✅ (프리미엄) | ✅ (프리미엄) | 보호자 영역 안 |
| 프리미엄 구독 체크 (RevenueCat) | ✅ | ✅ | ✅ |
| 표지 촬영/업로드 | ✅ | ✅ | ✅ |
| 계정 연결 (Google/Apple) | ✅ | ✅ | 보호자 영역 안 / 숨김 |
| 내보내기(백업) | ✅ | 단순화 | 숨김 |
| 고급 통계 | ✅ | 축소 | 숨김 |
| UI 특성 | 표준 | 큰 글씨·단계 축소 | 보호자 영역 + 고급 기능 숨김 |

> 게이팅에서 "영어 변형 비활성화" 항목은 제거됨. 남은 축은 프리미엄 구독 체크 + 아동 변형 보호자 영역 숨김.

- 공공도서관·위치는 구독(~3,000원/월) 프리미엄 기능.
- 위치 권한 선언은 기능을 켜는 변형의 android/ios에만 추가.

---

## 7. 표지(Storage) 설계 메모

- **외부 표지(네이버/구글 북스 URL)는 그대로 외부 링크로 두고**, 사용자가 직접 촬영한 표지만 Storage에 업로드. → 무료 1GB를 촬영분으로만 한정.
- 버킷 경로: `covers/{user_id}/{book_id}.webp`. `covers`는 **public 버킷**으로 확정(`cover_url`이 `/object/public/covers/` 경로로 발급됨).
- **결정(확정)**: `covers`는 public 버킷. 표지 이미지는 출판물 표지로 민감정보가 아니므로 공개 접근을 허용한다. 따라서 `covers`에는 Storage RLS(본인 폴더 제한)를 걸지 않는다 — public 버킷은 Storage RLS가 적용되지 않으며, 이는 의도된 동작이다.
- 행 수준 보안은 `books` 테이블 RLS가 전담한다. "누가 어떤 책을 소장하는가 / 메모 내용"은 §4의 `books` RLS로 보호되고, Storage는 표지 이미지 파일의 공개 URL 접근만 담당한다. 표지 파일 URL은 추측 불가능한 `{user_id}/{supabaseId}.webp`(둘 다 UUID) 조합이라 열거(enumeration)도 비현실적이다.
- 압축 포맷: **WebP q75**, 600×800 기준 실측 67.6KB (구 JPEG q85 143.9KB 대비 53% 감소). `cover_photo_service`(`CompressFormat.webp`) + `cover_upload_service`(`contentType: 'image/webp'`) 양쪽 일치. 기존 .jpg 표지와 버킷 내 혼재 — `cover_url`이 풀 경로를 저장하므로 렌더 무해.
- 펜딩 큐 + 온라인 복귀 자동 flush 패턴(`cover_upload_provider`)은 개념 유지. base64+Apps Script 부분만 `supabase.storage.from('covers').upload(...)`로 단순화.
- `cover_photo_service`의 촬영·리사이즈([1][2])는 유지, 업로드 단계만 교체.
- **불변(#11)**: cover 처리는 `supabaseId` 확보 후, `sync_queue` flush 완료 뒤 `promoteLocalCover`에서만 수행. `CoverUploadNotifier`에 독립 connectivity 구독 추가 금지(race 유발).

### 삭제 시 Storage 정리 (#14)

- **삭제 시 Storage 정리**: 책 행 삭제 시 `cover_url`에서 Storage 경로(`/object/public/covers/` 마커 기준)를 파싱해 표지 파일도 remove. 즉시 경로(`deleteBook` 온라인)와 큐 경로(`_flushDelete`) 양쪽에서 수행. 외부 표지(네이버/구글 URL)·null cover는 마커가 없어 remove 대상에서 제외(§7 원칙: 외부 표지는 Storage 미업로드).
- **불변**: Storage remove는 행 삭제 성공 후 **best-effort**로만 수행한다. remove 실패가 delete를 롤백하지 않는다(고아 파일 한둘 < 행 정합성). 확장자는 cover_url에서 그대로 보존(.jpg/.webp 혼재 대응).
- **알려진 한계**: 오프라인 삭제 후 네트워크 복귀 *직후* 첫 flush는 DNS 미조회(`Failed host lookup`)로 실패할 수 있다. 이 경우 delete는 큐에 보존되어 다음 flush(네트워크 재이벤트 또는 앱 시작 hook)에서 재시도·치유된다. 즉 "결국 정리"는 보장되나 즉시성은 보장되지 않는다.

---

## 8. 바코드 스캔 · ISBN 검색 설계 메모

스캔 진입점은 `scan_screen`(변형 앱 UI), ISBN 파싱·검색은 코어(`book_search_service`, 바코드 컨트롤러 로직 — §3)다. 과거 legacy(`bookshelf`) 구현에서 패치가 층층이 쌓여 회귀가 반복됐고, mylibrary에서 **명시적 상태머신**으로 재설계했다.

### 8.1 구현 개요
- `mobile_scanner` ^7.2.0 기반 ISBN 바코드 스캔 화면.
- ISBN 검색: **NAVER ISBN(`book_adv`) → 실패 시 Google Books(`isbn:`) 폴백** (`book_search_service.searchByISBN`). 텍스트 검색도 네이버→구글 동일 폴백.
- 검색 예외를 타입으로 구분: `BookSearchNetworkException`(연결 실패/타임아웃) / `BookSearchRateLimitException`(HTTP 429) / 기타. UI가 사유별 메시지로 분기.

### 8.2 상태머신 (`_Phase`, 8상태)
`scanning / processing / navigatingToAdd / cardNotFound / cardNetworkError / cardRateLimited / cardCancelled / cardError`
- 상태 전이는 단일 지점(`_to`)에서만 일어난다.
- 바코드 detect는 `scanning` 상태에서만 처리한다(단일 가드 `_phase != scanning`).
- 결과 카드 5종(`card*`)은 실패 사유별 메시지를 표시하고, 그 상태에서는 detect를 무시한다.

### 8.3 핵심 불변식 (회귀 방지 — 깨지 말 것)
1. **카메라 lifecycle은 `mobile_scanner` autoStart에 위임한다.** 수동 `start()`/`stop()` 미사용 (과거 이중 start로 인한 `controllerInitializing` 충돌의 원인). 화면 생존 동안 카메라는 계속 running.
2. **`detectionSpeed.normal` + 앱레벨 dedup(`_lastProcessedIsbn`)으로 중복 차단.** `noDuplicates`는 stop 없이 플랫폼 캐시를 못 비워 "같은 책 재스캔"을 막으므로 금지.
3. **dedup 락 해제(`_lastProcessedIsbn = null`)는 "다시 스캔" 버튼 단일 지점에서만.** 결과 카드 진입 전이와 저장 후 복귀는 모두 락을 유지한다 → 같은 책 자동 재검색·429 재유발 차단.
4. **`MobileScanner` 위젯은 항상 트리에 상주한다.** 어떤 상태에서도 조건부 제거 금지(제거 시 카메라 dispose). 오버레이(결과 카드·처리 표시)는 `Positioned`로 위에 쌓는다.

### 8.4 저장 / 취소 구분
- `add_book_screen`은 저장 시 `context.pop(true)`, 취소(back) 시 `pop(null)`을 반환한다.
- 검색결과 add·직접입력 add 모두 저장→`scanning`(락 유지), 취소→`cardCancelled`(락 유지). 시스템 back은 `PopScope(canPop: true)`로 어떤 상태에서도 스캔 화면을 벗어난다.

### 8.5 표지
- 외부 표지(네이버/구글 https URL)는 Storage 업로드 없이 `cover_url`에 직접 저장 (#11 불변 유지, §7 참조).

### 8.6 미완 / 백로그
1. **[완료]** 표지 미표시 원인 확인 및 해결.
   레거시 bookshelf 앱의 로컬 캐시 경로(`/data/user/0/com.bookshelf.bookshelf/...`)가
   CSV에 포함되어 임포트된 것이 원인. Supabase에서 직접 null 업데이트로 해결.
   재발 방지: `CsvImportService`에서 `http(s)://` 미시작 `cover_url`은 null 처리 추가.
2. **[완료]** flush DNS 실패 시 backoff 재시도 + 포그라운드 복귀 트리거 구현.
   DNS 실패(전체 항목 실패) 감지 시 3초 후 1회 자동 재시도.
   AppLifecycleState.resumed 시 triggerFlush() 호출(_isFlushing 가드로 중복 방지).
   실기기(Galaxy Z Flip 5) 검증 완료 — 오프라인 중 추가한 책 2권이 온라인 복귀 후
   Supabase insert + Storage 표지 promote까지 정상 완료.
3. **누적 고아 표지 정리(운영 과제)**: anon auth 특성상 세션 분리(재설치 등) 시 새 user_id 폴더가 생기고 이전 세션 표지가 Storage에 고아로 남는다(실측: covers에 b27e0ea7-... 등 이전 세션 폴더 잔존). 또한 delete 표지 정리가 best-effort라 복귀 직후 flush 실패분이 누적될 수 있다. linkIdentity 도입 전까지는 books.id에 없는 covers 파일을 주기적으로 식별·정리하는 과제가 남는다(현재는 대시보드 수동 정리로 대응).
4. **[완료]** 패키지명 `com.example.mylibrary` → `com.giltech.mylibrary` 변경 완료.
   `android/app/build.gradle.kts`, `AndroidManifest.xml`, `MainActivity.kt` 경로 변경.
   (2) 릴리즈 keystore SHA-1 등록 + (3) API 키 Android 앱 제한은 릴리즈 빌드 시점에 진행.

### 8.7 수기 입력 fallback (검색 실패 시 ISBN 프리필 등록)

**진단 정정**: 당초 §8.6-2에 "등록 불가"로 적었으나 실측 결과 진입 경로는 이미 존재한다. `_runSearch`의 예외 매핑이 4종 전부 card 상태로 떨어지고(429→`cardRateLimited`, network→`cardNetworkError`, 기타→`cardError`, 결과없음→`cardNotFound`), 5개 card 상태 모두 `_NotFoundCard`("직접 입력" 버튼 포함)를 재사용한다. 실제 마찰은 **검색 실패 시 ISBN을 프리필해 넘기는 경로가 없어, 사용자가 스캔한 ISBN을 수기로 다시 입력**해야 한다는 점이었다.

**설계**:
1. **진입**: 모든 card 상태의 "직접 입력" 버튼 → `_goToManualAdd`. (기존 유지)
2. **ISBN 프리필**: `_goToManualAdd`는 스캔된 ISBN이 있으면 인라인으로 `BookSearchResult(googleId:'', title:'', authors:const[], categories:const[], language:'', isbn13/isbn10: 스캔ISBN)`을 생성해 `/add`의 extra로 넘긴다. ISBN이 없으면 extra 없이 `/add` push(빈 폼). **`.manual` factory는 만들지 않는다** — 빈 껍데기 생성은 파싱 factory(`fromNaver`/`fromJson`)와 의미 층위가 다르고 호출처가 단일하므로, 코어 모델(`BookSearchResult`, §3)에 화면 로직을 들이지 않는다.
3. **저자 필수 유지(§9.1 정렬 보호)**: `add_book_screen`의 제목·저자 `_requiredValidator`는 그대로 둔다. 직접 입력은 ISBN만 프리필, 제목·저자는 수기. 저자 빈 문자열을 허용하면 `bookshelfCompare` 저자순 정렬이 더럽혀지므로(빈 문자열=4순위), acquisition 속도보다 정렬 데이터 품질을 우선한다. 직접 입력은 검색 실패 시의 소수 예외 경로이지 메인 플로우가 아니다.
4. **복귀(불변 §8.3-3 유지)**: 직접 입력은 검색결과 add와 동일한 복귀 규칙을 따른다 — `navigatingToAdd`로 전이(detect 무시) → push 결과로 분기: 저장(`pop(true)`)→`scanning`(락 유지), 취소(`pop(null)`)→`cardCancelled`(락 유지). **dedup 락 해제는 여전히 `_rescan` 단일 지점에서만**. 직접 입력 경로는 `_lastProcessedIsbn`을 건드리지 않는다 → 같은 책 자동 재검색·429 재유발 차단.

**구현 범위**: 실작업은 `_goToManualAdd` 한 메서드(ISBN 유무 분기 + 검색결과 add와 복귀 분기 통일). `add_book_screen`·`_NotFoundCard`·`BookSearchResult`·예외 매핑은 전부 무수정.

**[완료]** `_goToManualAdd`에 스캔 ISBN 프리필 구현 및 실기기 검증 완료.

---

## 8.8 C 작업 진행 상황

### 완료

- **C-1** Supabase 스키마 마이그레이션 — status rental 추가, is_read/medium/language/call_number 컬럼 추가
- **C-2** Drift 스키마 v4 — 동일 4개 컬럼 + migration addColumn 4건
- **C-3** 모델·화면 확장 — `Book` 도메인 모델 필드 추가, `add_book_screen` UI(매체 토글·읽은 책 스위치·청구기호), `book_detail_screen` 상세 표시, `book_list_screen` 상태 뱃지 3-way, `status rental` 전 화면 반영
- **C-4** CSV 내보내기 — CsvExportService, SettingsScreen 신규, /settings 라우트,
  AppBar 설정 아이콘, Android Download 폴더 직접 저장(API 29+, 폴백: 앱 캐시),
  share_plus 미사용(다운로드 직접 저장 + 완료 다이얼로그), iOS는 TODO 주석 보류
  (commit 527a079)
- **C-5** CSV 가져오기 — CsvImportService(인코딩 감지 UTF-8/CP949, 헤더 자동 매핑,
  중복 감지 ISBN 우선→제목+저자, 건너뛰기/덮어쓰기), CsvImportScreen(미리보기·
  체크박스·중복 뱃지·결과 화면), /csv-import 라우트, file_picker ^8.0.0 /
  charset_converter ^2.1.1 추가, 완료 후 목록 직행

### 완료 (추가)
- **C-6** Legacy 구글 시트 실데이터 검증 완료
  - 458권 중 457건 임포트 성공 (1건은 기존 중복으로 스킵)
  - CSV import 누락 필드 보완: language·priorityRead·description·표지URL 키워드 추가 (f57a821)
  - ISBN 지수 표기(9.79E+12) 정규화 추가 — 단, 구글 시트가 숫자 셀로 저장 시 정밀도 손실 발생.
    시트에서 ISBN 컬럼을 텍스트 형식으로 변경 후 재내보내기로 해결.

- **수정 저장 후 상세화면 미갱신 버그 fix** (b94f27e)
  - 원인: `ref.invalidate(booksProvider)` 후 즉시 `context.pop()` 호출 → provider가 AsyncLoading 상태에서 상세화면 복원
  - 수정: `invalidate` → `await ref.read(booksProvider.future)` → `pop` 순서로 변경

- **수정 모드 빈 문자열 필드 삭제 안 되는 버그 fix** (2e6fab9)
  - 원인: `_nonEmpty()` 헬퍼가 빈 문자열 → null 반환 → `copyWith(null)` → 기존 값 유지
  - 수정: 수정 모드 `copyWith` 호출부에서 `_nonEmpty()` 제거, 빈 문자열 직접 전달. `review`는 빈 문자열 허용(삭제 반영), 나머지 옵션 필드는 빈 문자열이면 null

- **상세화면 필드 순서 변경 + 수정화면 읽은책 토글 우측정렬** (2987597)
  - 상세화면: 출판연도·출판사·책장 위치·청구기호·ISBN·매체·장르·총 페이지 순서 확정
  - 수정화면: `Row(mainAxisAlignment: MainAxisAlignment.end)` 적용으로 토글 우측 정렬

- **집계 탭 폐기 결정**: 별도 집계 탭 계획 → 통계 탭(§10)으로 대체. `stats_screen`이 단일 진입점.
- **"서재" → "서가"/"나의 도서관" 용어 전체 교체** (결정 #18) — 탭 라벨·탐색 맥락은 "서가", 화면 제목·문구는 "나의 도서관"
- **탭 구조 홈/서가/통계/설정 4개로 확장** (결정 #17)

### "나의 도서관" 명칭 확정

앱 내 서재 공간의 공식 명칭을 **"나의 도서관"**으로 확정한다.

**철학적 배경**: 나심 탈레브의 안티라이브러리(antilibrary) 개념에 기반한다. 읽지 않은 책은 결핍이 아니라 가능성이다. 미독을 부정적으로 표현하는 문구는 앱 어디에도 사용하지 않는다. 통계 화면(§10)의 동적 문구가 이 철학을 전면에 드러낸다.

- UI 표시: "서가"(탭 라벨), "나의 도서관"(화면 제목·AppBar 문구). "서재"는 전면 폐기 (결정 #18)
- 코드/문서: "나의 도서관" 또는 "my library"
- 반드시 피할 표현: "읽지 않은 책이 N권 있습니다", "아직 N권을 못 읽었습니다" 등 미독을 결핍으로 묘사하는 모든 문구

- **book_card UI 개선** (이번 세션)
  - 하단 행: 상태 칩 옆 `Icons.bookmarks_outlined` + location 값 표시
    (location null/빈 값이면 미표시)
  - 우선읽기: star 아이콘 → Transform.scale Switch 토글로 변경
    (off: muted, on: gold #C8A96E, 상태 칩과 동일 높이, 우측 끝 정렬)
  - togglePriorityRead Drift 로컬 전용 유지 확인 (결정 #24)

- **Google 계정 연결 (linkIdentity)** (이번 세션)
  - `AuthService.linkGoogle()` → `linkIdentity(OAuthProvider.google, redirectTo: 'io.supabase.mylibrary://login-callback')`
  - `AuthNotifier` (AsyncNotifier) — `build()`에서 `onAuthStateChange` 스트림 구독, `ref.onDispose`로 해제
  - 설정 화면 계정 섹션: loading / error / linked / unlinked 4상태
  - AndroidManifest.xml: `io.supabase.mylibrary://login-callback` intent-filter 추가
  - 개발용 로그아웃 버튼 (`kDebugMode` 무관하게 표시, 출시 전 제거 예정)
  - 상세 §16

- **서가 화면 필터/정렬 + location 자동완성** (이번 세션)
  - BookFilter 모델 신규: statuses/attributes/media/initial/locations/sortBy
  - 바텀 시트 필터 패널 (DraggableScrollableSheet)
  - 첫 글자: 한글/알파벳/숫자 드롭다운 accordion (커스텀, 외부 패키지 없음)
  - AppBar tune 아이콘 + 활성 필터 수 뱃지
  - 속성 필터 라벨: 우선 / 읽음 / 아직 읽지 않음
  - AppBar actions 순서: 바코드 스캔 / 검색 / 추가
  - location 자동완성: 기존 입력값 1개↑이면 드롭다운 제안
  - 상세 §15

### import 설계 확정 (C-5)

| 항목 | 결정 |
|---|---|
| 중복 감지 기준 | ISBN 일치 우선 → 없으면 제목+저자 동일 |
| 미리보기 | 컬럼 매핑 확인 후 행 단위 체크박스로 선택 import |
| 인코딩 | UTF-8 기본, CP949(EUC-KR) 자동 감지 후 변환 |
| 컬럼 매핑 | 헤더 키워드 자동 인식(제목/title, 저자/author 등), 미인식 시 드롭다운 수동 지정 |
| 중복 처리 | 사용자 선택(건너뛰기/덮어쓰기) |

---

## 9. 제거 / 폐기 대상 (기존 코드)

- `services/sheets_service.dart` (시트 동기화)
- `apps_script/Code.gs`, `apps_script/cover_upload.gs`
- 302 리다이렉트 수동 추적, `text/plain` CORS 우회 로직
- `cover_upload_service.dart`의 base64→Apps Script→Drive→lh3 사슬
- `services/database_service.dart` (sqflite) → Drift로 대체
- `provider` 기반 상태관리 → riverpod
- SharedPreferences 키 `sheets_webhook_url`, `sheets_sync_enabled`
- Google Sheets는 **선택적 내보내기(export-only)로만** 잔존 가능 (1차 저장소 아님)

---

---

## 10. 통계 화면 (stats_screen)

### 구조: 통계 홈 + 드릴다운 5개

통계 탭(`/stats`)은 **홈 화면** + **드릴다운 화면 5개**로 구성된다.

```
/stats          → StatsScreen       (통계 홈, 하단 탭 3번째)
/stats/genre    → StatsGenreScreen  (장르 지형도)
/stats/year     → StatsYearScreen   (출판연도 분포)
/stats/author   → StatsAuthorScreen (가장 많이 만난 저자)
/stats/monthly  → StatsMonthlyScreen(월별 책과의 만남)
/stats/wishlist → StatsWishlistScreen(오래 기다린 책)
```

드릴다운은 `/stats` 하위 GoRoute로 정의 → 하단 탭 유지, 뒤로가기로 홈 복귀.

---

### 통계 홈 (`/stats`)

한 화면 안에 들어오는 구성. `ListView` (항목 고정, 스크롤 허용).

| 항목 | 위젯 | 조건 |
|------|------|------|
| 섹션 1 | 안티라이브러리 대시보드 | 항상 |
| 섹션 2 | 매체별 분포 + 언어별(조건부) | 항상 |
| 드릴다운 카드 | 장르 지형도 | 항상 |
| 드릴다운 카드 | 출판연도 분포 | 항상 |
| 드릴다운 카드 | 가장 많이 만난 저자 | 항상 |
| 드릴다운 카드 | 월별 책과의 만남 | 항상 |
| 드릴다운 카드 | 오래 기다린 책 | wishlist 책 있을 때만 |

드릴다운 카드: 제목 + 미리보기 텍스트 + chevron_right → `context.push(route)`.

**미리보기 텍스트 로직:**

- **장르 지형도**: `"10개 영역 중 N곳 탐험 · 가장 넓은 곳은 X"` / KDC 없으면 `"아직 지도를 그릴 수 없습니다"`
- **출판연도**: 최빈 버킷(미상 제외) → `"2010년대 책이 가장 많습니다"` / 연도 정보 없으면 `"연도 정보가 없습니다"`
- **저자**: 모든 저자가 1권이면 `"다양한 저자의 책을 소장하고 있습니다"` / 아니면 `"TOP저자 외 N명의 저자"`
- **월별**: tracking 미설정 → `"지금부터 기록을 시작할 수 있습니다"` / 설정 시 → `"X월부터 기록 중 · 총 N권"`
- **희망**: 가장 오래 기다린 책 제목 + `"N일째 기다리는 중"`

---

### 섹션 1 — 안티라이브러리 대시보드

- 전체 N권, 읽음/미독 progress bar
- 소장 / 희망 / 대여 / 우선읽기 카운트 칩 (4개 Expanded)

  | 칩 | 전경색 | 배경색 |
  |------|--------|--------|
  | 소장 | gold `#C8A96E` | goldSubtle |
  | 희망 | muted `#7A7060` | mutedSubtle |
  | 대여 | `#5B7FA6` | `#5B7FA6` 15% alpha |
  | 우선 | red `#E74C3C` | `#E74C3C` 15% alpha |

- 미독 비율 동적 문구:
  - 80%+: "당신의 도서관은 무한한 가능성을 담고 있습니다"
  - 50–79%: "당신의 도서관에는 N권의 가능성이 있습니다"
  - 30–49%: "도서관의 절반이 아직 당신을 기다립니다"
  - 30% 미만: "깊이 읽어내는 도서관입니다"
- **불변**: 미독을 결핍으로 표현하는 문구 절대 금지

---

### 섹션 2 — 매체별 + 언어별

- 종이책/전자책/오디오북 3칸 카드 (권수 + %)
- 언어 행(조건부): language 필드 20% 이상 + 'ko' 단일이 아닐 때만 표시.
  상위 4개 언어 + 기타 집계. 텍스트 형식: `"한국어 300 · 영어 60 · 기타 5"`

---

### 드릴다운 — 장르 지형도 (`/stats/genre`)

**철학**: 진행률/완독률이 아니라 지도 은유. 안티라이브러리에서 "비어 있는 영역"은
결핍이 아니라 "아직 가보지 않은 곳"이므로, 정렬은 인기순이 아니라 **KDC 순서
고정**(대분류 0~9, 총류→역사) — 지도이므로 항목이 매번 같은 위치에 있어야
사용자가 자기 서가의 지형을 학습할 수 있다.

- 최상단: 대분류 10개를 고정 순서로 나열한 세로 막대 능선(`_RidgeBar`, 최대
  높이 80px + 라벨). 0권인 대분류도 dim 테두리로 표시(탭 비활성) — 연도 드릴다운의
  "0권도 표시" 관례와 동일.
- 능선 막대 탭(1권 이상) → DraggableScrollableSheet(0.6/0.4/0.92)로 해당
  대분류의 중분류(강목) breakdown 표시. 중분류도 KDC 코드 순서 고정, 0권은 dim.
- 중분류 행 탭(1권 이상) → 책 목록 시트(공용 `_BooksSheet`, 기존 연도/저자
  드릴다운과 동일한 표지+제목+저자 리스트, 탭 시 `/books/:id`)
- KDC 없는 책은 대분류 능선에서 제외하고 하단에 "아직 분류되지 않은 책 N권"
  행으로 별도 표시(탭 → 책 목록). 결핍 표현 대신 중립적 문구 사용.
- 대분류/중분류 매핑은 `lib/utils/kdc_genre.dart`(§24) 재사용 —
  `kdcMainClassesOrdered`, `kdcDivisionsOf(mainKey)` 헬퍼 신규 추가.
- 처분된 책은 다른 드릴다운과 동일하게 집계에서 제외 (§25).

---

### 드릴다운 — 출판연도 분포 (`/stats/year`)

**1년 단위** 가로 막대. 0권 연도도 표시(dim 색, 탭 비활성). 최댓값 기준 상대 비율.

범위: `books 중 year 최솟값 ~ max(현재연도, year 최댓값)` 내림차순. 미상(year null/빈) 맨 끝.
상단 설명: `"YYYY년부터 올해까지"` + Divider (year 데이터 있을 때만).
행 탭 → DraggableScrollableSheet (0.5/0.3/0.9) 해당 연도 책 목록.

---

### 드릴다운 — 가장 많이 만난 저자 (`/stats/author`)

**2권 이상** 소장 저자만 필터. 권수 내림차순 + 동점 가나다순 → **TOP 30** 가로 막대.
30명 초과 시 맨 끝에 "외 N명" 행(탭 없음).
2권 이상 저자 없으면 "모든 저자의 책을 1권씩 소장하고 있습니다" (목록·구분선 미표시).
상단 설명: `"2권 이상 같은 저자인 경우"` + Divider.
저자 행 탭 → DraggableScrollableSheet (0.5/0.3/0.9) 해당 저자의 책 목록.

---

### 드릴다운 — 월별 책과의 만남 (`/stats/monthly`)

- `effectiveDate = acquired_at ?? created_at`
- `tracking_started_at` 이후 책만 집계 → 최근 12개월 가로 막대
- 미설정: "지금부터 나의 도서관에 새로 들어오는 책의 흐름을 기록합니다" + [오늘부터 시작] 버튼
  → `profiles.tracking_started_at = today` 저장 (ProfileRepository.setTrackingStartedAt)

---

### 드릴다운 — 오래 기다린 책 (`/stats/wishlist`)

status = 'wishlist' TOP 20. 경과일 = 오늘 − created_at. 탭 → `/books/:id`.

---

### 공통 위젯 (`stats_common.dart`)

드릴다운 화면 공유 위젯 + 함수.

| 항목 | 설명 |
|------|------|
| `StatsBarRow` | 라벨 90px + 가로 막대 Expanded + 권수 32px |
| `StatsSectionCard` | 패딩 16, borderRadius 12, dim 0.5px 테두리 |
| `StatsSectionTitle` | muted 12sp w600 letterSpacing 0.5 |
| `StatsSmallCover` | 36×50 px 책 표지 썸네일 |
| `yearBucket()` | String? → 5년 버킷 문자열 |
| `yearBucketOrder` | 버킷 정렬 순서 상수 리스트 |
| `langNames` | 언어 코드 → 한국어 표기 맵 |

---

### 구현 제약

- 차트: `Container` 비율 막대. **외부 패키지 금지** (fl_chart 등).
- 팔레트: 기존 다크 골드 팔레트 준수 (AppColors.*).
- 데이터: `booksProvider`(Drift 로컬) + `trackingStartedProvider`(Supabase profiles).

---

### 조건부 렌더링 규칙

| 항목 | 표시 조건 | 숨김 시 |
|------|-----------|---------|
| 언어별 분포 | language 필드 입력 비율 20% 이상 + 'ko' 단일이 아닐 때 | 행 전체 미표시 |
| 월별 추이 | `tracking_started_at` 설정 시 | 설정 유도 카드 표시 |
| 오래 기다린 책 | wishlist 책 1권 이상 | 드릴다운 카드 숨김 |

---

### 보류

| 항목 | 사유 |
|------|------|
| 출판사 분포 | 장르 지형도 구현 완료 후 재검토 |

---

---

## 11. 홈 화면 (`/home`)

### 철학

앱 첫 인상. "나의 도서관"이라는 정체성을 매일 확인하는 공간.
antilibrary 철학 — library 개념을 개인화. 독립서점처럼 큐레이션이 곧 정체성. "나의 도서관"은 세계관.
미독은 결핍이 아니라 가능성 — 이 철학이 홈 화면 전체 문구·톤에 스며든다.

---

### 탭 구조 (결정 #17)

```
하단 탭 4개: [홈] [서가] [통계] [설정]
  index 0 → /home        HomeScreen
  index 1 → /            BookListScreen
  index 2 → /stats       StatsScreen (+ 하위 드릴다운)
  index 3 → /settings    SettingsScreen
initialLocation: '/home'
```

### 화면 구조

```
Scaffold(body: Stack(fit: expand))
  ├─ 레이어1: 배경 — Image.file(home_bg_{n}.webp) 1장(랜덤) 또는 ColoredBox(`#0F0E0C`)
  ├─ 레이어2: LinearGradient 오버레이 (투명→0xCC→불투명, stops 0/0.5/1.0)
  └─ 레이어3: SafeArea → SingleChildScrollView → Column
       ├─ 헤더 (도서관 이름 + 편집 아이콘 + "N권 · D일째")
       ├─ 카드1: 오늘의 책 (미독 소장본 날짜 시드 랜덤)
       ├─ 카드2: 요약 (읽음/미독 권수 + 가장 많이 만난 저자)
       └─ 카드3: 기다린 지 오래된 책 (wishlist 중 앱 시작마다 랜덤 1권, 선택적)
```

### 도서관 이름 (library_name)

- `profiles.library_name TEXT` — Supabase upsert
- 홈 헤더는 **표시 전용**: 소제목 "묵책서가"(muted 12sp, 브랜드명 고정) + 큰 이름
  (cream 22sp bold, 미설정 시 "나의 도서관")
- **편집은 설정 화면으로 이동** (홈 헤더의 연필 아이콘 제거). 설정 → 최상단
  "도서관 이름" 섹션 탭 → AlertDialog + TextField, 빈 값 저장 시 null 처리.
  설정에서 바꾸면 `libraryNameProvider`를 통해 홈 헤더에 즉시 반영
- 상태: `LibraryNameNotifier extends AsyncNotifier<String?>` in providers.dart

### 오늘의 책 (카드1)

- 대상: `!isRead && status == 'owned'` 목록
- 랜덤 시드: `year * 10000 + month * 100 + day` (날짜 고정, 매일 바뀜)
- 미독 소장본 없으면 카드 미표시

### 나의 도서관 요약 (카드2)

- 읽음 N권 / 미독 N권 (Row 양끝)
- 가장 많이 만난 저자: 2권 이상 저자 中 최다 → 1명 표시. 없으면 미표시.

### 기다린 지 오래된 책 (카드3)

- `status == 'wishlist'` 목록 중 `Random().nextInt()` — seed 없음, 앱 시작마다 랜덤 1권
- "N일째 기다리는 중" (today − createdAt + 1)
- wishlist 없으면 카드 미표시
- 탭 → `/books/:id`

### D일째 계산

- `books` 전체 `createdAt` 최솟값 기준
- `today.difference(earliest).inDays + 1`
- `createdAt` 있는 책이 없으면 미표시

### 불변

- **오늘의 책**: 날짜 seed(`year * 10000 + month * 100 + day`) 기준 당일 고정. 같은 날이면 항상 같은 책.
- **기다린 지 오래된 책**: `Random().nextInt()` — seed 없음, 앱 시작마다 랜덤. 당일 고정 아님.

---

### 배경 이미지 (HomeBackgroundNotifier)

위치: `lib/features/home/home_background_notifier.dart`

- 저장 경로: `getApplicationDocumentsDirectory()/home_bg_{0~2}.webp` (최대 3슬롯)
- 앱 시작 시 `Random().nextInt(슬롯수)`로 1장 선정. seed 없음 — 매 시작마다 랜덤.
- 0장이면 기본 다크 배경(`#0F0E0C`).
- `pickAndSave(slot)`: `ImagePicker` 갤러리 → `FlutterImageCompress.compressWithFile` webp, 80→60→40 순, 500KB 이내 시 저장
- `remove(slot)`: 해당 슬롯 파일 삭제 + state 갱신
- 설정 화면: `[홈 배경 이미지]` 섹션 — 슬롯별 100×60 썸네일 + [추가/교체] + [삭제]

---

---

## 15. 서가 화면 (book_list_screen)

### book_card 하단 행 구조

- 상태 칩: 소장(gold) / 희망(muted) / 대여(#5B7FA6)
- 우선읽기 Switch: off=muted, on=gold(#C8A96E), Transform.scale로 칩 높이 맞춤
- togglePriorityRead: Drift 로컬 전용 (결정 #24)

---

### 필터 패널 (BookFilterSheet)

바텀 시트 (DraggableScrollableSheet, initial 0.75 / min 0.5 / max 0.95).
헤더: "필터 및 정렬" + [초기화] 버튼. 하단 고정 [적용] 버튼(gold 배경).

#### 그룹 구성 및 조건

| 그룹 | 항목 | 내부 조건 | 선택 방식 |
|------|------|-----------|-----------|
| 정렬 | 등록일 역순(기본) / 제목순 / 저자순 / 연도 역순 / 책장 위치순 | — | 단일 선택 |
| 상태 | 소장 / 희망 / 대여 | OR | 복수 선택 |
| 속성 | 우선 / 읽음 / 아직 읽지 않음 | OR | 복수 선택 |
| 매체 | 종이책 / 전자책 / 오디오북 | OR | 복수 선택 |
| 첫 글자 | 한글(ㄱ~ㅎ) / 알파벳(A~Z) / 숫자(0~9) | — | 단일 선택 |
| 장르 | 서가에 존재하는 KDC 대분류만 KDC 순서로 동적 표시 (1개↑일 때만 섹션 노출) | OR | 복수 선택 |
| 서가 위치 | DB 내 location 유니크 값 동적 표시 (1개↑일 때만 섹션 노출) | OR | 복수 선택 |
| 기타 | 처분된 책 보기 | — | 단일(배타 뷰, §25) |

그룹 간 AND. 정렬은 activeCount 뱃지 제외. 장르 필터는 `kdc` 필드 첫 자리(대분류
키 '0'~'9')로 매칭 — 매핑은 `kdc_genre.dart`(§24) 재사용.

#### BookFilter 모델

```dart
class BookFilter {
  final Set<String> statuses;    // 'owned','wishlist','rental'
  final Set<String> attributes;  // 'priority','read','unread'
  final Set<String> media;       // 'paper','ebook','audio'
  final String? initial;         // 초성 단일값 or null
  final Set<String> locations;   // location 값들
  final Set<String> genres;      // KDC 대분류 키 '0'~'9'
  final String sortBy;           // 기본: 'createdAt'

  int get activeCount =>
    statuses.length + attributes.length + media.length +
    (initial != null ? 1 : 0) + locations.length;
}
```

#### 정렬 로직

| sortBy 값 | 정렬 기준 |
|-----------|-----------|
| `createdAt` | created_at 내림차순 (기본) |
| `title` | bookshelfCompare(title) |
| `author` | bookshelfCompare(author) |
| `year` | year 내림차순, null 맨 끝 |
| `location` | bookshelfCompare(location), null 맨 끝 |

#### 첫 글자 드롭다운 (커스텀 accordion)

- 한글 / 알파벳 / 숫자 3개 Row 배치
- 탭 → AnimatedContainer로 그리드 펼침
- 다른 드롭다운 탭 시 현재 닫힘
- 선택 시 다른 두 드롭다운 선택 해제 (단일 선택 보장)
- 선택값 있으면 라벨: "한글 · ㄴ" + ✕ (그룹 해제)
- 외부 패키지 없음

#### 칩 스타일

- 미선택: surface2 배경 / muted 텍스트 / dim 0.5px 테두리
- 선택: gold 15% alpha 배경 / gold 텍스트 / gold 테두리
- 정렬 선택: gold 배경 / bg 텍스트

---

### AppBar 구성

- actions 순서: [바코드 스캔] [검색] [필터(tune)] [추가]
- 필터 버튼: Icons.tune_rounded + 활성 필터 수 CircleAvatar 뱃지
  (gold 배경, bg 텍스트, radius 8, 활성 0개이면 미표시)

---

### location 자동완성 (add_book_screen)

- 조건: DB 내 location 값 1개↑ 존재 시 드롭다운 표시
- 필터: 현재 입력값으로 시작하는 값만 (startsWith, 대소문자 무시)
- 데이터 소스: BooksNotifier.allLocations (booksProvider state에서 유니크 집계)
- UI: TextField 바로 아래 Material card (surface2, elevation 4)
- 닫힘 조건: 항목 탭(자동완성 적용) 또는 TextField focus 해제

---

## 16. Google 계정 연결 설계

### 확정 방식 — `linkIdentity` 브라우저 redirect

| 비교 항목 | signInWithIdToken (폐기) | **linkIdentity (확정)** |
|---|---|---|
| user_id | 새 계정 생성 가능 | **익명 user_id 유지 보장** |
| nonce | Android google_sign_in ^6.x 미지원 | 불필요 |
| 외부 패키지 | `google_sign_in` 필요 | 불필요 |
| 완료 시점 | await 직후 | deep link → onAuthStateChange 이후 |

### 코드 구조

```dart
// AuthService.linkGoogle()
await Supabase.instance.client.auth.linkIdentity(
  OAuthProvider.google,
  redirectTo: 'io.supabase.mylibrary://login-callback',
);

// AuthNotifier (AsyncNotifier<bool>)
// build(): onAuthStateChange 스트림 구독 → linked 여부로 state 갱신
// linkGoogle(): loading 세팅 후 AuthService.linkGoogle() 호출
//   → 브라우저 열림 후 즉시 반환
//   → deep link 수신 후 onAuthStateChange(signedIn/userUpdated) 발생
//   → state 자동 갱신 (linkGoogle()에서 수동 갱신 없음)
```

### Supabase 대시보드 필수 설정

| 항목 | 위치 | 값 |
|---|---|---|
| Google provider 활성화 | Authentication → Sign In / Providers → Google | Enable ON |
| Client IDs | 동일 | 웹 + Android 클라이언트 ID (쉼표 구분) |
| Client Secret | 동일 | 웹 클라이언트 Secret |
| Allow manual linking | Authentication → Sign In / Providers → User Signups | ON |
| Redirect URL | Authentication → URL Configuration | `io.supabase.mylibrary://login-callback` |

### Android 설정

```xml
<!-- AndroidManifest.xml — MainActivity 안 -->
<intent-filter>
  <action android:name="android.intent.action.VIEW" />
  <category android:name="android.intent.category.DEFAULT" />
  <category android:name="android.intent.category.BROWSABLE" />
  <data android:scheme="io.supabase.mylibrary" android:host="login-callback" />
</intent-filter>
```

- deep link 처리: `supabase_flutter`가 `app_links` (transitive dep)를 통해 자동 수신·처리
- PKCE flow: `gotrue-2.21.0` 기본값 (`AuthFlowType.pkce`) — 명시 불필요

### 설정 화면 계정 섹션 (4상태)

| 상태 | 표시 |
|---|---|
| loading | `CircularProgressIndicator` |
| error | 빨간 에러 메시지 |
| 미연결 | [Google 계정 연결] 버튼 + 안내 문구 |
| 연결 완료 | ✓ 이메일 + "나의 도서관이 동기화되고 있습니다" |

### 출시 전 체크리스트

- [ ] 설정 화면 로그아웃 버튼 `kDebugMode` 가드 또는 제거
- [ ] 릴리즈 SHA-1 → Google Cloud Console OAuth 클라이언트에 등록
- [ ] Google Books API 키 Android 앱 제한 설정

---

---

## 17. 통합 검색 화면 (unified_search_screen)

### 구조
/search — UnifiedSearchScreen

AppBar: 검색창 (단일 TextField)

TabBar: [도서 검색] [가까운 도서관 검색]

TabBarView:

탭 0 — 도서 검색 (네이버/구글 Books API)

탭 1 — 가까운 도서관 검색 (도서관정보나루 API)

### 진입점

| 진입점 | 전달값 | 동작 |
|---|---|---|
| 서가 AppBar 🔍 아이콘 | 없음 | 도서 검색 탭 기본 |
| 도서 상세 시트 [가까운 도서관에서 이 책 찾아보기] | isbn | 도서관 탭 자동 전환 + 검색 |
| 희망도서 상세 [가까운 도서관에서 이 책 찾아보기] | isbn | 도서관 탭 자동 전환 + 검색 |

### 라우터

```dart
GoRoute(
  path: '/search',
  builder: (context, state) {
    final p = state.uri.queryParameters;
    return UnifiedSearchScreen(
      initialTab:   p['tab'] == 'library' ? 1 : 0,
      initialIsbn:  p['isbn'],
      initialQuery: p['query'],
    );
  },
),
```

### 도서 검색 탭

- 네이버 → 구글 Books 폴백 (기존 BookSearchService 재사용)
- 결과 카드 탭 → DraggableScrollableSheet (60%~92%)
- 시트 내용: 표지 + 제목/저자/출판사/연도/ISBN + 책 소개(4줄 제한, 더 보기/접기)
- 즉시 저장: [희망도서로 추가] / [소장으로 추가] → repo.addBook() 직접 호출, /add 미경유
- 저장 완료 후 버튼 영역 → "서가에 추가했습니다 · 상태" 로 전환
- 이미 서가에 있는 책 → "이미 서가에 있습니다 · 상태" 표시
- 중복 방지: 저장 전 booksProvider에서 ISBN/title+author 대조
- [가까운 도서관에서 이 책 찾아보기]: ISBN 있을 때만 표시, 탭 전환 + 검색

### 가까운 도서관 검색 탭

- 도서관정보나루 `libSrchByBook` API
- 엔드포인트: `http://data4library.kr/api/libSrchByBook`
- API 키: `LIBRARY_API_KEY` (env/dev.json)
- region 파라미터: GPS 좌표 → `regionCodeFromLatLng()` 자동 변환 (lib/utils/region_code.dart)
- `libSrchByBook`은 소장 도서관만 반환 (hasBook 필터 불필요)
- GPS: geolocator ^13.0.0, 권한 거부 시 도서관 목록만 표시 (거리 컬럼 없음)
- 결과 카드: 도서관명 + 주소 + 거리(GPS 있을 때)
- 초기 안내: "도서 검색 탭에서 [가까운 도서관에서 이 책 찾아보기]를 눌러보세요"
- 소장관 없을 때: "이 지역에 소장한 도서관이 없습니다"

### 신규 파일

| 파일 | 역할 |
|---|---|
| lib/models/library_search_result.dart | 도서관 검색 결과 모델 |
| lib/services/library_search_service.dart | 도서관정보나루 API (searchByIsbn, searchNearby) |
| lib/utils/region_code.dart | GPS 좌표 → 지역코드 변환, 지역코드 → 지역명 |
| lib/screens/unified_search_screen.dart | 통합 검색 화면 |

### Android 추가 설정

```xml
<!-- AndroidManifest.xml -->
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
<!-- application 태그 -->
android:usesCleartextTraffic="true"
```

---

## 18. 서가 화면 로컬 검색창

- 서가 화면 본문 상단 상주 (AppBar 아님)
- 대상: 로컬 Drift 실시간 필터 전용
- 매칭: title / author / location (소문자 substring)
- 구현: BookFilter.searchQuery 필드 추가 + BookFilterNotifier.setSearchQuery()
- filteredBooksProvider의 applyBookFilterAndSort에 searchQuery 조건 삽입
- 기존 필터(상태/매체/첫 글자 등)와 AND 조건으로 함께 동작
- × 버튼으로 초기화 시 bookFilterProvider.setSearchQuery('') 호출

---

## 19. Supabase 중복 insert 사고 기록 (2026-06-27)

### 증상
flutter run 실행마다 Supabase books 테이블에 469권씩 중복 insert (469 → 938).

### 원인
로컬 Drift의 supabaseId가 Supabase 실제 uuid와 불일치한 상태에서
reconcileLocalOnlyToRemote()가 앱 시작마다 실행되어 469권 전부를 orphan으로 판정,
Supabase에 재insert.

불일치 발생 경위:
1. 최초 중복 발생 (reconcile 이중 실행 추정) → Supabase 934권
2. 중복 행 SQL DELETE (created_at 기준 나중 것 삭제)
3. 삭제된 행의 uuid가 로컬 supabaseId에 잔존
4. 앱 재시작 시 reconcile이 잔존 uuid를 orphan 판정 → 재insert 반복

### 해결
Supabase books 전체 삭제 → reconcile 실행 → 로컬 supabaseId 기준으로 재insert.

### 교훈 및 재발 방지

**중복 발생 시 올바른 정리 순서:**
1. Supabase 전체 삭제 (`DELETE FROM books WHERE user_id = '...'`)
2. 앱 재시작 → reconcile이 로컬 기준으로 재insert
3. COUNT 확인

**절대 하지 말 것:**
- Supabase에서 중복 행만 골라 DELETE 후 앱 재시작
  → 삭제된 uuid가 로컬에 잔존 → reconcile이 재insert → 무한 반복

**_saveBook 중복 방지 (결정 #27)**
unified_search_screen의 즉시 저장 시 booksProvider에서 ISBN/title+author 대조 후
중복이면 addBook 호출 없이 기존 status를 UI에 반영.

---

## 20. KDC / DDC / LC 분류기호 (Drift v6)

### 배경
도서관 서가 찾기 및 분류 정보 제공을 위해
KDC(한국십진분류기호) / DDC(듀이십진분류법) /
LC(미국의회도서관분류법) 3개 필드를 추가.
기존 callNumber(사용자 입력 청구기호)와 별도로 관리.

### 스키마 변경

**Drift v6 migration:**
```dart
if (from < 6) {
  await m.addColumn(books, books.kdc);
  await m.addColumn(books, books.ddc);
  await m.addColumn(books, books.lc);
}
```

**Supabase:**
```sql
ALTER TABLE books ADD COLUMN IF NOT EXISTS kdc TEXT;
ALTER TABLE books ADD COLUMN IF NOT EXISTS ddc TEXT;
ALTER TABLE books ADD COLUMN IF NOT EXISTS lc  TEXT;
```

### 취득 방법

| 필드 | 취득 방법 |
|---|---|
| kdc | 도서관정보나루 srchDtlList API 자동 취득 (ISBN 기준) |
| ddc | 수동 입력 (add_book_screen 고급 필드) |
| lc  | 수동 입력 (add_book_screen 고급 필드) |

### KDC 자동 취득 시점
- 바코드 스캔 / 도서 검색으로 추가 시: initState에서 백그라운드 취득
- 기존 도서 백필: 설정 화면 [데이터 관리] → [KDC 자동 채우기] 버튼
  (1회성 작업 완료 후 버튼 제거 예정)

### add_book_screen UI
- KDC: 기본 표시 필드 (자동취득 or 수동)
- DDC / LC: 우측 정렬 토글 [DDC / LC (선택)] → 펼치면 입력 필드

### 표시 방식
있는 것만 표시 (book_detail_screen _infoRows):
  ('KDC', b.kdc)
  ('DDC', b.ddc)
  ('LC',  b.lc)

---

## 21. 홈 화면 카드 데이터 소스 확정

| 카드 | 데이터 소스 | 선택 방식 |
|---|---|---|
| 서가로부터 오늘의 추천 | 희망도서 (status == 'wishlist') | 날짜 시드 고정 랜덤 (매일 바뀜) |
| 좀 오래 묵은 책 | 미독 소장본 (owned && !isRead) | 시드 없는 랜덤 (앱 시작마다 바뀜) |

### 변경 이력
- 초기 설계: 오늘의 추천=미독소장본, 묵은책=희망도서
- 확정 변경: 두 카드의 데이터 소스 교체

---

## 22. 도서 검색 / 도서관 검색 화면 분리

### 최종 구조

| 화면 | 경로 | 진입점 |
|---|---|---|
| 도서 검색 | /book-search | 서가 AppBar 🔍 아이콘 |
| 도서관 검색 | /search | 도서 상세 시트 / 희망도서 상세 [가까운 도서관에서 이 책 찾아보기] |

### 도서관 검색 화면 기능
- 위치 선택: [현재 위치 GPS] / [직접 입력 (Nominatim)]
- 직접 입력: 구/동 단위 텍스트 → 좌표 변환 → 지역코드 자동 결정
- 도서관 카드 탭 → 상세 모달
  - 소장 여부 / 대출 가능 여부 (bookExist API)
  - 한국십진분류기호 (srchDtlList API, ISBN당 1회 캐시)
  - 안내: "도서관에 따라 일치하지 않을 수 있습니다."
  - [지도 보기]: 카카오맵 딥링크 → 구글맵 폴백

### 신규 파일
| 파일 | 역할 |
|---|---|
| lib/screens/book_search_screen.dart | 도서 검색 전용 화면 |
| lib/screens/unified_search_screen.dart | 도서관 검색 전용 화면 |
| lib/services/geocoding_service.dart | Nominatim 주소→좌표 변환 |

---

## 23. UI 개선 이력 (이번 세션)

| 항목 | 내용 |
|---|---|
| 책 상세 수정/삭제 버튼 | 반투명 배경 (black 35%) 추가 |
| 책 상세 뒤로가기 버튼 | 동일한 반투명 배경으로 통일 |
| 서가 AppBar 제목 | libraryNameProvider 기반 동적 표시 |
| 설정 화면 섹션 순서 | 홈 배경 이미지 → 데이터 관리 → 계정 |
| 홈 화면 헤더 | 텍스트 그림자 추가 (가독성) |
| 홈 카드 라벨 | 서가로부터 오늘의 추천 / 좀 오래 묵은 책 |
| 홈 요약 카드 | 전체 N권(대) + 미독 N권, 저자 제거 |

---

## 24. 장르 정규화 — KDC 런타임 파생 확정 (결정 #27)

### 배경 — 원안 폐기

당초 "네이버 category로 장르 정규화" 계획이었으나 실측 결과 네이버 Books 검색 API
(`book.json` / `book_adv.json`)는 category 필드를 제공하지 않는다
(제공 필드: title/link/image/author/discount/publisher/pubdate/isbn/description
9개뿐 — category가 있는 건 네이버 쇼핑 API이며 책 검색 API가 아님). 대안 비교 결과
KDC(이미 §20에서 보유·백필 인프라 구축됨)를 장르 소스로 채택.

### 결정 3개 (Q1/Q2/Q3)

**Q1 — 저장 방식: 런타임 파생 (Drift/Supabase 스키마 변경 없음)**
- `books.kdc` 필드에서 대분류(1자리)·중분류(2자리)를 매 호출 시 계산
- `lib/utils/kdc_genre.dart` — `kdcToGenre(String? kdc) → KdcGenre?`
  - `KdcGenre.main`: 대분류 10구분 (총류~역사)
  - `KdcGenre.sub`: 중분류 100구분 (강목). 미사용 구간은 매핑에서 제외 → main만 표시
  - `.label`: `"문학 · 한국문학"` 형식
- **KDC 계층 확인 결과**: 1자리(주류, 10구분) → 2자리(강목, 100구분) → 3자리(요목,
  1000구분, 예: 813=한국소설) → 소수점 이하(세목, 예: 813.6=한국 현대소설). 즉 KDC는
  중분류보다 세분화된 요목(3자리)·세목(소수점)도 제공하지만, 이번 결정 범위는
  대분류+중분류까지로 한정(Q3 참조). 요목 이하는 추후 필요 시 `kdc_genre.dart`에
  레벨만 추가하면 됨 (매핑 테이블 확장으로 충분, 구조 변경 불필요)

**Q2 — 기존 genre 필드 처리: 전량 삭제 후 자유 태그로 전환**
- 기존 수동 입력값(120권, 27종, 체계 없음 — `뇌과학/뇌과학 학습/학습 뇌과학` 혼재,
  `'202'` 같은 무의미값 등)은 재사용 불가 판정, 전량 삭제
- `genre` 컬럼/필드 자체는 유지하되 의미를 "사용자 자유 태그"로 재정의
  (Drift/Supabase 스키마 변경 없음 — 값만 초기화)
- 설정 → 데이터 관리 → [기존 장르 값 초기화 (1회성)] 버튼으로 실행 완료
  (`settings_screen.dart` `_resetGenre` — KDC 백필과 동일 패턴, `repo.updateBook`
  경유로 로컬 Drift + Supabase 양쪽 반영). §20의 KDC 백필과 달리 **1회성** —
  실행 완료 확인 후 버튼·함수 제거됨 (null 문자열 정리 버튼과 동일한 운영 방식)
- 삭제는 빈 문자열(`''`)로 기록 (모델 `copyWith`가 `field ?? this.field` 패턴이라
  `null` 전달 시 기존값 유지됨 — §8.8 review 필드 버그 fix와 동일 원리로 빈 문자열 사용)

**Q3 — 중분류 매핑 범위: 보유 도서 검토 없이 KDC 표준 100개 전체 하드코딩**
- `kdc_genre.dart`의 100구분 매핑은 한국십진분류법(KDC) 6판 공식 강목표 기준
  (출처: 한국도서관협회 KDC 6판 요목표) — 사용자 보유 469권의 실제 분포 확인 없이
  표준 전체를 채택
- `[미사용]` 지정 구간(26x 일부, 61, 79 일부 등)은 매핑에서 제외 → 해당 kdc는
  대분류만 표시

### UI 반영

| 화면 | 변경 |
|---|---|
| `book_detail_screen.dart` `_infoRows` | `('장르', kdcToGenre(b.kdc)?.label)` — KDC 파생값으로 교체. `('태그', b.genre)` 신규 추가 — 기존 genre 필드(자유 입력) |
| `add_book_screen.dart` | genre 컨트롤러 라벨 `'장르'` → `'태그 (자유 입력)'`, 힌트 `"예) 힐링, 인생책, 다시 읽고 싶은"` 추가. 필드 자체(`_genre` 컨트롤러, DB 컬럼)는 변경 없음 |

> ⚠️ **이 UI 반영표는 §26(결정 #29)로 대체됨.** 장르는 이제 KDC 드롭다운 구조화
> 입력, genre 컬럼은 "태그 자유입력"이 아니라 "내 분류"(장르 종속 복수 태그)로
> 재규정됐다. 상세화면 표현·add 화면 필드 모두 §26 기준을 따른다.

### 보류 — 이번 범위 밖

- KDC 미취득 도서(§20 백필 실패분)는 장르 없음 — 통계 화면 조건부 렌더링(§10) 규칙 적용
- 요목(3자리)·세목(소수점) 단위 세분화, 외국서 대응 강화는 추후 과제
- §10 통계 화면 "장르 지형도" 섹션은 이번 결정으로 데이터 소스 확보됨 → 별도 작업으로 착수 가능 (보류 해제 조건 충족)

---

## 25. disposed(처분) 상태 — A안 확정 (결정 #28)

### 배경

책 처분(판매/기부/분실 등) 이력을 관리하는 기능. 4순위 항목 중 첫 착수.

### 결정 4개

**저장 방식 — A안: 별도 `disposedAt` 필드, status는 'owned' 유지**

B안(status에 'disposed' 4번째 값 추가)과 비교 검토 후 A안 채택. 근거:
- status 전이가 단순하게 유지됨 (owned/wishlist/rental 3-way 그대로) — 기존
  ToggleButtons·BookFilter 하드코딩 지점을 4-way로 넓힐 필요 없음
- "처분 취소(되찾음)" 나중에 구현 시 `disposedAt = null`만 지우면 원래 상태가
  자동 복원됨 — B안은 처분 직전 상태(owned/rental 등)를 별도로 기억해야 함
- "한때 소장했던 이력을 보존한다"는 프레이밍에 자연스러움
- 단점(소장 여부 판정 시 status만으로는 부족, disposedAt도 같이 봐야 함)은
  `Book.isActiveOwned` 파생 getter로 흡수 — 소장 여부를 판정하는 모든 코드는
  `status == 'owned'`를 직접 보지 말고 반드시 이 getter를 사용한다

**처분 사유 — 날짜만 기록**

`disposedAt`(TIMESTAMPTZ)만 저장. 판매/기부/분실 같은 구체적 사유는 기존
`review`(메모) 필드에 자유 기술 — 별도 enum 필드 추가하지 않음.

**노출 방식 — 기본 숨김, 필터로 켜면 처분된 책만 배타적으로 보기**

`BookFilter.showDisposed`(기본 false)로 제어.
- false(기본): `disposedAt != null`인 책 자동 제외, 나머지는 기존 상태/속성/매체/
  위치 필터 그대로 적용
- true: 처분 이력 전용 뷰로 전환 — 처분된 책만 배타적으로 보여주고 다른
  상태/속성/매체/위치 필터는 무시(적용 안 함). 검색어(searchQuery)는 그대로 적용

필터 시트 "기타" 섹션에 "처분된 책 보기" 칩으로 켤 수 있음. (초기 구현은
"함께 보임" 방식이었으나 실기기 테스트 후 "배타적 전용 뷰"로 수정 확정)

**통계 반영 — 제외 + "처분 N권" 별도 표시**

홈 화면 요약 카드("전체 N권")·통계 화면 전체(안티라이브러리 대시보드, 출판연도·
저자·월별·희망 드릴다운 4개)에서 처분된 책 제외. 안티라이브러리 대시보드에
"처분한 책 N권"을 조건부(N>0)로 별도 표시.

### 스키마

**Drift v7:**
```dart
DateTimeColumn get disposedAt => dateTime().nullable()();
// migration: if (from < 7) await m.addColumn(books, books.disposedAt);
```

**Supabase (아직 미실행 — 대시보드 SQL Editor에서 직접 실행 필요):**
```sql
ALTER TABLE books ADD COLUMN IF NOT EXISTS disposed_at TIMESTAMPTZ;
```

> ⚠️ Drift v7으로 스키마 버전을 올렸으므로 `flutter run` 전 반드시
> `dart run build_runner build --delete-conflicting-outputs`로
> `app_database.g.dart`를 재생성해야 한다 (수기 편집 금지 파일).

### Book 모델

```dart
final DateTime? disposedAt;   // null = 소장 중

bool get isActiveOwned => status == 'owned' && disposedAt == null;
bool get isDisposed => disposedAt != null;
```
`copyWith`는 acquiredAt과 동일한 sentinel(`_absent`) 패턴으로 `disposedAt: null`
명시 설정(처분 취소)을 지원한다.

### UI

| 화면 | 변경 |
|---|---|
| `book_detail_screen.dart` | AppBar에 처분/처분취소 아이콘(소장 중이거나 이미 처분된 책만 노출). 처분 시 확인 다이얼로그 → `disposedAt = DateTime.now()`. 처분된 책은 상태 토글 아래 "처분됨 · 날짜" 표시 |
| `book_filter_sheet.dart` | "기타" 섹션에 "처분된 책 보기" 토글 칩 신규 |
| `home_screen.dart` | `activeBooks`(disposedAt==null) 기준으로 오늘의 책·요약 카드·희망도서 카드 전부 재계산 |
| `stats_screen.dart` + 드릴다운 4개(`stats_year/author/monthly/wishlist_screen.dart`) | 각 화면 진입점에서 `books.where((b) => !b.isDisposed)`로 제외. 안티라이브러리 대시보드에 처분 N권 조건부 표시 |
| `csv_export_service.dart` | `disposed_at` 컬럼 추가 (백업 목적, 이력 보존) |

### 실기기 검증 이력

- [x] Supabase `disposed_at` 컬럼 추가 SQL 실행 완료
- [x] `dart run build_runner build --delete-conflicting-outputs` 실행 후 실기기 검증 완료
  - 콜드 스타트 후에도 처분 직후 서가 목록에서 정상 제외 확인
  - "처분된 책 보기" 배타적 모드로 수정 후 정상 동작 확인
- [x] git commit (커밋 해시는 §0 결정 요약 및 git log 참조)

### 트러블슈팅 기록 (실기기 검증 중 발견)

1. **Supabase 컬럼 누락 상태에서의 오검증**: `disposed_at` 컬럼을 Supabase에
   추가하기 전에 처분 기능을 테스트하면, 로컬 Drift에는 즉시 반영되지만
   이후 `syncFromRemote()`(당겨서 새로고침 등)가 컬럼 없는 원격 데이터로
   로컬 행을 덮어써 disposedAt이 도로 null이 되는 것처럼 보였다. 컬럼 추가
   후 재검증하여 해결 확인. **교훈**: 신규 컬럼을 추가하는 기능은 Supabase
   ALTER TABLE을 먼저 실행하고 나서 실기기 테스트해야 오검증을 피한다.
2. **필터 배타성 재확정**: 최초 구현은 `showDisposed=true`일 때 기존 필터에
   처분된 책을 "추가로" 얹는 방식이었으나, 실기기 테스트 후 "처분된 책만
   배타적으로 보기"로 요구사항이 확정되어 `applyBookFilterAndSort` 로직을
   수정함 (다른 상태/속성/매체/위치 필터는 무시, 검색어만 유지).

---

## 26. 장르 구조화 입력 + "내 분류" 종속 태그 (결정 #29)

### 배경 — §24의 진화

§24는 장르를 KDC에서 파생(읽기 전용)하고 genre 컬럼을 자유 태그로 재정의했다.
그러나 KDC 미취득 도서 약 90권은 장르가 비어 있어 **수동 입력이 필수**가 됐다.
방안 A(자유 텍스트 override, 스키마 신설)와 방안 B(구조화 입력, KDC 필드 재사용)를
비교한 결과 **방안 A = 구조화 입력**을 채택한다 — 통계(장르 지형도 §10)·필터(§15)가
모두 `kdc` 첫 자리를 키로 쓰므로, 장르 입력이 kdc에 구조적으로 쓰이면 정합성이
자동 유지되고, §24가 폐기한 자유 텍스트 혼재 문제로 회귀하지 않는다.

> ⚠️ 용어 주의: 이 절의 "방안 A/B"는 설계 논의 당시 라벨이며, 최종 채택안은
> "KDC 필드에 구조화 입력하는 방식"이다. Drift/Supabase **스키마 변경 없음**.

### 결정 — 장르 구조화 입력

**입력 위젯**: 대분류 드롭다운(10개, KDC 순서 고정) + 중분류 드롭다운(선택,
`kdcDivisionsOf(대분류키)`로 동적). 선택 결과를 `kdc`에 1자리(대분류만) 또는
2자리(중분류까지) 프리픽스로 저장.

**불변식 2개 (반드시 지킬 것)**:
1. **미리 선택**: 드롭다운은 현재 `kdc`에서 파생한 값으로 초기 선택돼야 한다
   (빈 칸이 아니라 "지금 이 책은 이렇게 분류돼 있다"를 보여주고 수정하는 형태).
2. **정밀 코드 보존**: 사용자가 실제로 선택을 **바꿨을 때만** `kdc`를 새로 쓴다.
   손대지 않으면 기존 정밀 코드(예: `813.6`, srchDtlList 자동 취득분)를 그대로
   보존한다 — 코스한 드롭다운(예: `81`)이 소수점 분류를 뭉개지 않게.

**정밀 코드 직접 입력**: 기존 원시 KDC 텍스트 입력(예: `813.6`)은 DDC/LC 토글과
같은 접이식 "고급" 영역으로 이동. 일반 사용자는 드롭다운, 파워유저는 접이식 원시 입력.

**자동 KDC와의 우선순위**: add 화면의 ISBN 기반 백그라운드 KDC 자동 취득은
"init 시점에 kdc가 비어 있을 때만" 유지(기존 `_fetchKdcInBackground` 가드). 사용자가
드롭다운을 건드리면 그 선택이 authoritative.

### 결정 — "내 분류" 종속 태그

§24가 자유 태그로 재정의한 genre 컬럼을 **"내 분류"**(장르에 종속된 사용자 세부
라벨)로 다시 규정한다. 메모(review, 산문형 독립 코멘트)와 구분되는, 장르 문맥 안의
짧은 재사용 라벨.

- **복수 허용**: genre 컬럼에 **쉼표 구분**으로 여러 태그 저장(`SF,디스토피아`).
  파싱: 쉼표 split → trim → 빈 값 제거. CSV 내보내기는 csv 패키지가 쉼표 포함 필드를
  자동 인용하므로 무해. genre 컬럼은 §24에서 전량 초기화됐으므로 깨끗한 시작.
- **UI 라벨**: "태그" → **"내 분류"**.
- **장르 종속 (2중 결합)**:
  1. *그룹핑* — add/edit에서 "내 분류" 입력을 장르 드롭다운 바로 아래 같은 섹션에 배치.
  2. *맥락 자동완성* — 태그 입력 시 **같은 대분류의 다른 책에 쓰인 태그**를 제안
     (location 자동완성 §15 패턴 재사용). 대분류 미선택 상태(90권 등)면 전체 태그에서
     제안, 대분류 선택되면 그 대분류로 스코프. 자유 텍스트인데도 장르 맥락 제안으로
     혼재를 억제.
- **상세화면 표현**: 장르 경로 뒤에 **인라인 칩**. 예: `문학 > 한국소설 · #SF #디스토피아`.
  기존 별도 `('태그', ...)` 행은 제거. kdc 파생 장르가 없으면(미분류) 칩만 표시.

### 스키마

**변경 없음.** `kdc` 컬럼(장르 소스), `genre` 컬럼(쉼표 구분 "내 분류") 모두 기존 재사용.
Drift/Supabase migration·build_runner 불필요.

### UI 반영 (구현 대상)

| 화면 | 변경 |
|---|---|
| `book_detail_screen.dart` | `_infoRowsMain`의 `('장르', pathLabel)` 행을 **장르 경로 + 인라인 칩** 전용 위젯으로 교체. `_infoRowsTail`의 `('태그', b.genre)` 행 제거 |
| `add_book_screen.dart` | "태그 (자유 입력)" 단일 필드 제거. 장르 대분류/중분류 드롭다운(미리 선택 + 변경 시만 kdc 재작성) + 그 아래 "내 분류" 멀티 태그 입력(칩 add/remove + 대분류 맥락 자동완성) 신설. 원시 KDC 입력은 접이식 고급 영역으로 이동 |
| `providers.dart` | 대분류별 태그 집계 헬퍼(자동완성 소스) — `booksProvider`의 genre를 쉼표 파싱해 kdc 대분류별로 그룹핑 |

### 실기기 검증 대상 (Claude Code 인계)

- 미분류 90권에서 드롭다운으로 장르 지정 → 상세·지형도·필터에 즉시 반영되는지
- 정밀 코드(813.6) 보유 책을 편집화면에서 저장(드롭다운 미변경) 시 813.6 보존되는지
- 드롭다운 변경 시에만 kdc가 바뀌는지
- "내 분류" 복수 칩 저장·표시(인라인 `· #a #b`), 같은 대분류 태그 자동완성
- CSV 내보내기/가져오기에서 쉼표 포함 genre 라운드트립

---

_갱신 규칙: 결정 변경 시 이 문서를 먼저 고치고 커밋한다. 코드와 문서가 어긋나면 문서가 기준이며, 어긋난 코드를 문서에 맞춘다._
