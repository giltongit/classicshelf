# 내서재 (mylibrary) — 공통 아키텍처 (개정본)

> 이 문서는 기존 `Bookshelf` 단일 앱 코드(FEATURES.md 스냅샷)를 **Supabase 기반 · 로컬 우선 · 내서재 멀티변형** 목표 아키텍처로 옮기기 위한 마이그레이션 기준 문서다.
> Claude Code 세션은 이 문서를 canonical source로 삼는다. 결정이 바뀌면 이 문서를 먼저 갱신하고 커밋한다.

---

## 0. 이번 개정에서 확정된 결정 요약

| # | 결정 | 영향 |
|---|---|---|
| 1 | 로그인 추가 — Supabase **Anonymous Auth** 기본, Google/Apple 선택적 연결(linkIdentity) | 익명 세션으로 마찰 없이 시작, 후일 계정 연결 시 `user_id` 유지 |
| 2 | Google Sheets/Apps Script 폐기 → **Supabase(PostgreSQL)** | `sheets_service.dart`, `Code.gs`, 302 추적, text/plain 우회 전부 제거 |
| 3 | Google Drive 표지 업로드 폐기 → **Supabase Storage** | base64→Apps Script→Drive→lh3→setSharing 사슬 제거 |
| 4 | 로컬 DB **sqflite → Drift** | 오프라인 유지, Supabase를 source of truth로 하는 3계층 동기화 |
| 5 | 상태관리 **provider → flutter_riverpod** | 목표 스택 정렬 |
| 6 | **공공도서관 자료 검색 + 위치 안내** 기능을 **코어에 편입** (신규) | 한국 이용자 공공도서관 접근성 = 앱 철학의 일부 |
| 7 | **영어 iOS 변형은 이번 범위에서 분리** (별도 제품 트랙) | 변형 3종(일반/시니어/아동)으로 축소 |
| 8 | iOS는 "미뤄둔 숙제" — 한국인용 앱이라도 추후 동일 3변형에 추가 | 코어는 지금부터 순수 Dart로 격리 유지 |
| 9 | 세션 시작 시 `supabaseId` 기준 로컬→원격 reconcile 자동 실행 (`reconcileLocalOnlyToRemote`) | 큐 수동 정리 중 원격 row 유실 방어. `SyncQueueFlusher.build()`에서 flush 직후 fire-and-forget. 멱등 — 원격 존재 시 스킵, `23505 unique_violation` 무시 |
| 10 | 5c-2c LWW(Last-Write-Wins) 충돌 해소는 `linkIdentity` 도입까지 보류 | `updated_at` 컬럼으로 충돌 시점 파악은 준비됨. 멀티 기기 본격 지원(소셜 계정 연결) 시점에 구현 |
| 11 | 오프라인에서 추가된 책의 cover는 `supabaseId` 확보 후에만 업로드·교체. `flushSyncQueue()`(book insert → supabaseId 발급) 완료 직후 `promoteLocalCover`가 `localId→supabaseId` 경로로 cover 업로드 + cover_url 원격 반영 | `CoverUploadNotifier`의 독립 connectivity 구독 제거 — cover flush는 항상 sync_queue flush 완료 후 순서 실행(race 방지). **불변: cover 업로드·cover_url 교체는 supabaseId 없이 하지 않는다** |
| 12 | 바코드 스캔 화면(`scan_screen`)을 **명시적 상태머신**(`_Phase` 8상태)으로 구현. 카메라 lifecycle은 `mobile_scanner` autoStart에 위임(수동 start/stop 미사용), `detectionSpeed.normal` + 앱레벨 dedup(`_lastProcessedIsbn`)으로 중복 차단 | 무한 재검색 루프·시스템 back 무반응 회귀 차단. **불변: dedup 락 해제는 "다시 스캔" 단일 지점에서만**. 상세 §8 |
| 13 | `covers` 버킷 **public 확정** — 표지는 비민감 정보, Storage RLS 미적용. 행 보안은 `books` RLS 전담 | Storage 정책 단순화, signed URL 불필요. 상세 §7 |
| 14 | 책 삭제 시 Storage 표지 파일 정리(best-effort, 즉시·큐 양경로). 외부URL·null 제외, 확장자 보존 | 행 삭제와 표지 정리 분리로 인한 고아 파일 방지. 복귀 직후 flush 실패는 재시도로 치유 |
| 15 | **모델 확장** — `status`에 `rental` 추가, 신규 필드 `is_read` / `medium` / `language` / `call_number` | `is_read`는 안티라이브러리 통계용, UI에서 조용하게 처리(미독을 결핍으로 표시 안 함). `medium` 기본값 `'paper'`. `language` 자동 감지(C-3 이후). 책장 위치·청구기호 자동완성(3회 이상 입력 시) — C-3 확정 |
| 16 | **홈 탭 신규 추가(4탭)** + `ProfileRepository` `library_name` 확장 + `HomeBackgroundNotifier` | 하단 탭 홈/서가/통계/설정 4개. `initialLocation: '/home'`. 홈 화면: Stack(배경이미지+그라디언트+카드3개). 도서관 이름 편집(Supabase profiles.library_name). 배경 이미지: image_picker + flutter_image_compress webp 500KB 이내. |

### 작업 순서 (의존성 기준)
```
② Supabase 프로젝트 + 스키마/RLS
  → ① 인증 (auth.uid()가 있어야 RLS 성립)
  → ③ Storage (인증 의존)
  → 단일 앱을 Supabase 위에서 안정화 (Android, 일반 변형)
  → ④ 코어 추출 (mylibrary_core) + 변형 분기
  → (별도 트랙) iOS 플랫폼 추가 / 영어 변형
```
> ④를 먼저 하면 아직 흔들리는 데이터 계층을 패키지 경계로 굳혀 버리는 실수가 생긴다. 데이터 계층 안정화가 코어 추출의 선행 조건이다.

---

## 1. 목표 스택

| 항목 | 결정 |
|---|---|
| 백엔드 | Supabase (PostgreSQL + Storage + Anonymous Auth) |
| 인증 | Supabase Anonymous Auth + Google/Apple linkIdentity |
| 로컬 DB | **Drift** (로컬 캐시, Supabase=source of truth) |
| 상태관리 | flutter_riverpod |
| 아키텍처 | Clean Architecture, Feature-First |
| 모델 | freezed (immutable) |
| 네트워크 | dio |
| 라우팅 | go_router |
| 기타 | mobile_scanner ^7.2.0, speech_to_text, flutter_image_compress |
| 결제 | RevenueCat (구독 ~3,000원/월) |

### 3계층 데이터 흐름
```
UI (riverpod) ──▶ Repository (코어)
                     ├─ Drift (로컬 캐시 / 오프라인 읽기)
                     └─ Supabase (source of truth)
                          ▲
                          └─ 오프라인 동기화 큐 (온라인 복귀 시 flush)
```

---

## 2. 변형 · 플랫폼 매트릭스

### 이번 개발 범위
| 변형 | Android (now) | iOS (later) |
|---|---|---|
| 일반 (general) | ✅ | ⏳ 숙제 |
| 시니어 (senior) | ✅ | ⏳ 숙제 |
| 아동 (children) | ✅ | ⏳ 숙제 |

- **영어 iOS**: 이 매트릭스 밖의 **별도 제품 트랙**. 이번 범위에 포함하지 않는다.
- iOS는 "안 함"이 아니라 "지금 안 하지만 반드시 할 것". 동일 3변형을 코어 재사용으로 추가한다.

### iOS 트랙에서 처리할 숙제 (FEATURES.md 17장 + 위치 기능 추가분)
- `Info.plist`: 카메라 / 사진 권한 항목
- `Info.plist`: `NSLocationWhenInUseUsageDescription` (위치 기능 코어 편입으로 추가)
- CocoaPods 설정
- **Apple 로그인 추가** — 소셜 로그인 제공 시 Apple 로그인은 심사 의무. iOS 출시 시점에 반드시 포함. (Android 단독 단계에서는 후순위)

---

## 3. 코어 / 변형 책임 분할

기준: **"오디언스가 달라도 똑같은가"** → 같으면 코어, UI/게이팅이면 변형.

### `mylibrary_core` (순수 Dart, 플랫폼 의존 금지)
- **모델**: `Book`, `BookSearchResult`, `Library`, `LoanAvailability` (freezed)
- **데이터 계층**: Supabase 리포지토리 + Drift 로컬 캐시 + 오프라인 동기화 큐
- **인증 서비스**: 익명 세션, Google/Apple linkIdentity 흐름
- **도서 검색**: 네이버 → 구글 북스 폴백
- **공공도서관**: `LibraryRepository` 인터페이스 + 한국 구현체 (§5)
- **위치/거리**: GPS 좌표 획득 추상화, 거리 계산, 가까운 순 정렬
- **표지**: 촬영/리사이즈 로직 (FEATURES [COVER][1][2] 단계), Storage 업로드
- **바코드**: ISBN 파싱·컨트롤러 로직 (스캔 화면 상태머신은 변형 앱 UI, 컨트롤러·검색은 코어 — §8)
- **유틸**: `sort_utils`, `cover_color`, `ReadingStatus`/`SyncState` 등 enum

> ⚠️ **권한 선언은 코어에 못 넣는다.** 코어는 순수 Dart라 Android `Manifest`·iOS `Info.plist`가 없다. 로직은 코어, 권한 선언은 변형 앱마다 각자의 android/ios 폴더에.

### 변형 앱 (general / senior / children)
- `screens/`, `widgets/` — UI
- 테마(팔레트·타이포), `main.dart`, 라우터
- **기능 게이팅** (§6)
- 플랫폼 분기 — 권한 선언, 파일 경로, 카메라/스캐너 설정

### 모노레포 vs 멀티레포 (결정 필요)
- 기존 방향: 변형별 별도 Git 저장소 + 코어를 git/path 의존성으로
- 변형 3개로 늘면 코어 버전 동기화가 번거로워짐 → **melos 모노레포**(코어 1 + 변형 N)가 버전 핀 고통을 줄인다
- **저장소를 쪼개기 전에 둘 중 무엇으로 갈지 명시적으로 결정할 것** (현재 미확정)

---

## 4. Supabase 스키마 / RLS

### 스키마 변경 이력

| 버전 | 변경 내용 |
|---|---|
| Drift v1-v3 | 기본 books 테이블 + sync_queue |
| Drift v4 | is_read, medium, language, call_number, status rental 추가 |
| Drift v5 | books.acquired_at DATE 추가 |
| — | profiles 테이블 신규 (tracking_started_at) |
| — | profiles.library_name TEXT 컬럼 추가 |

### Supabase profiles 테이블 (신규)

```sql
CREATE TABLE IF NOT EXISTS public.profiles (
  user_id uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  tracking_started_at DATE,
  library_name TEXT
);
ALTER TABLE public.profiles ENABLE ROW LEVEL SECURITY;
CREATE POLICY "select_own" ON public.profiles FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "insert_own" ON public.profiles FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "update_own" ON public.profiles FOR UPDATE
  USING (auth.uid() = user_id)
  WITH CHECK (auth.uid() = user_id);
```

**library_name 컬럼 추가 (결정 #16)**

```sql
ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS library_name TEXT;
```

### acquired_at 컬럼 추가

```sql
ALTER TABLE books ADD COLUMN IF NOT EXISTS acquired_at DATE;
```

- 의미: 사용자가 해당 책을 만난(취득한) 날짜. add/edit 화면에서 선택적 입력.
- 통계 섹션 6(월별 책과의 만남)에서 `effectiveDate = acquired_at ?? created_at`로 사용.

```sql
create table public.books (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  title text not null,
  author text not null,
  isbn text,
  cover_url text,
  description text,
  status text not null default 'owned' check (status in ('owned','wishlist','rental')),
  review text,
  page_count int,
  year text,
  genre text,
  publisher text,
  location text,
  priority_read boolean not null default false,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  is_read boolean not null default false,
  medium text not null default 'paper' check (medium in ('paper','ebook','audio')),
  language text,
  call_number text
);

alter table public.books enable row level security;

-- ⚠️ SELECT/INSERT/UPDATE/DELETE 네 정책을 반드시 각각 정의. 하나라도 빠지면 사고.
create policy "select_own" on public.books for select using (auth.uid() = user_id);
create policy "insert_own" on public.books for insert with check (auth.uid() = user_id);
create policy "update_own" on public.books for update using (auth.uid() = user_id) with check (auth.uid() = user_id);
create policy "delete_own" on public.books for delete using (auth.uid() = user_id);
```

- 기존 시트의 `upsert/delete/list/stats` 4개 액션 → 위 4개 정책 + Supabase 클라이언트 호출로 대체
- `priority_read`는 시트엔 컬럼이 없어 로컬 전용이었으나, 이제 동기화 가능
- **C-3 신규 컬럼** — `is_read`, `medium`, `language`, `call_number`, `status` 에 `rental` 추가: Drift v4 마이그레이션 완료, Supabase 콘솔에서 ALTER TABLE로 동기 반영 필요

---

## 5. 공공도서관 — `LibraryRepository` (코어)

영어 변형을 뺐으므로 이 추상화의 동기는 **"다국가 대응"이 아니라 "한국 내 복수 도서관 데이터 소스 대응"**이다. 도서관정보나루·KOLIS-NET이라는 서로 다른 소스를 다루고, 한쪽 API가 죽었을 때 폴백하거나 소스를 교체할 여지를 남긴다 (네이버→구글 폴백과 같은 논리).

```dart
// mylibrary_core
abstract interface class LibraryRepository {
  /// 좌표 기준 근처 도서관 (거리순 정렬은 코어 위치 로직에서 처리)
  Future<List<Library>> findNearbyLibraries({
    required double lat,
    required double lng,
    double radiusKm = 5,
  });

  /// 특정 도서관의 해당 ISBN 소장/대출 가능 여부
  Future<LoanAvailability> checkAvailability({
    required String libraryCode,
    required String isbn,
  });
}

// 한국용 구현체: 도서관정보나루 + KOLIS-NET (폴백/병합)
class KoreaLibraryRepository implements LibraryRepository { /* ... */ }
```

- 위치 권한 선언(`ACCESS_FINE/COARSE_LOCATION`, iOS `NSLocationWhenInUseUsageDescription`)은 §3 원칙대로 **변형 앱**에.
- 기능을 끄는 변형/플랫폼은 권한도 빼서 불필요한 위치 권한 요구를 막는다.

---

## 6. 변형별 권한 · 게이팅 매트릭스

| 항목 | 일반 | 시니어 | 아동 |
|---|---|---|---|
| 공공도서관·위치 검색 | ✅ (프리미엄) | ✅ (프리미엄) | 보호자 영역 안 |
| 프리미엄 구독 체크 (RevenueCat) | ✅ | ✅ | ✅ |
| 표지 촬영/업로드 | ✅ | ✅ | ✅ |
| 계정 연결 (Google/Apple) | ✅ | ✅ | 보호자 영역 안 / 숨김 |
| 내보내기(백업) | ✅ | 단순화 | 숨김 |
| 고급 통계 | ✅ | 축소 | 숨김 |
| UI 특성 | 표준 | 큰 글씨·단계 축소 | 보호자 영역 + 고급 기능 숨김 |

> 게이팅에서 "영어 변형 비활성화" 항목은 제거됨. 남은 축은 프리미엄 구독 체크 + 아동 변형 보호자 영역 숨김.

- 공공도서관·위치는 구독(~3,000원/월) 프리미엄 기능.
- 위치 권한 선언은 기능을 켜는 변형의 android/ios에만 추가.

---

## 7. 표지(Storage) 설계 메모

- **외부 표지(네이버/구글 북스 URL)는 그대로 외부 링크로 두고**, 사용자가 직접 촬영한 표지만 Storage에 업로드. → 무료 1GB를 촬영분으로만 한정.
- 버킷 경로: `covers/{user_id}/{book_id}.webp`. `covers`는 **public 버킷**으로 확정(`cover_url`이 `/object/public/covers/` 경로로 발급됨).
- **결정(확정)**: `covers`는 public 버킷. 표지 이미지는 출판물 표지로 민감정보가 아니므로 공개 접근을 허용한다. 따라서 `covers`에는 Storage RLS(본인 폴더 제한)를 걸지 않는다 — public 버킷은 Storage RLS가 적용되지 않으며, 이는 의도된 동작이다.
- 행 수준 보안은 `books` 테이블 RLS가 전담한다. "누가 어떤 책을 소장하는가 / 메모 내용"은 §4의 `books` RLS로 보호되고, Storage는 표지 이미지 파일의 공개 URL 접근만 담당한다. 표지 파일 URL은 추측 불가능한 `{user_id}/{supabaseId}.webp`(둘 다 UUID) 조합이라 열거(enumeration)도 비현실적이다.
- 압축 포맷: **WebP q75**, 600×800 기준 실측 67.6KB (구 JPEG q85 143.9KB 대비 53% 감소). `cover_photo_service`(`CompressFormat.webp`) + `cover_upload_service`(`contentType: 'image/webp'`) 양쪽 일치. 기존 .jpg 표지와 버킷 내 혼재 — `cover_url`이 풀 경로를 저장하므로 렌더 무해.
- 펜딩 큐 + 온라인 복귀 자동 flush 패턴(`cover_upload_provider`)은 개념 유지. base64+Apps Script 부분만 `supabase.storage.from('covers').upload(...)`로 단순화.
- `cover_photo_service`의 촬영·리사이즈([1][2])는 유지, 업로드 단계만 교체.
- **불변(#11)**: cover 처리는 `supabaseId` 확보 후, `sync_queue` flush 완료 뒤 `promoteLocalCover`에서만 수행. `CoverUploadNotifier`에 독립 connectivity 구독 추가 금지(race 유발).

### 삭제 시 Storage 정리 (#14)

- **삭제 시 Storage 정리**: 책 행 삭제 시 `cover_url`에서 Storage 경로(`/object/public/covers/` 마커 기준)를 파싱해 표지 파일도 remove. 즉시 경로(`deleteBook` 온라인)와 큐 경로(`_flushDelete`) 양쪽에서 수행. 외부 표지(네이버/구글 URL)·null cover는 마커가 없어 remove 대상에서 제외(§7 원칙: 외부 표지는 Storage 미업로드).
- **불변**: Storage remove는 행 삭제 성공 후 **best-effort**로만 수행한다. remove 실패가 delete를 롤백하지 않는다(고아 파일 한둘 < 행 정합성). 확장자는 cover_url에서 그대로 보존(.jpg/.webp 혼재 대응).
- **알려진 한계**: 오프라인 삭제 후 네트워크 복귀 *직후* 첫 flush는 DNS 미조회(`Failed host lookup`)로 실패할 수 있다. 이 경우 delete는 큐에 보존되어 다음 flush(네트워크 재이벤트 또는 앱 시작 hook)에서 재시도·치유된다. 즉 "결국 정리"는 보장되나 즉시성은 보장되지 않는다.

---

## 8. 바코드 스캔 · ISBN 검색 설계 메모

스캔 진입점은 `scan_screen`(변형 앱 UI), ISBN 파싱·검색은 코어(`book_search_service`, 바코드 컨트롤러 로직 — §3)다. 과거 legacy(`bookshelf`) 구현에서 패치가 층층이 쌓여 회귀가 반복됐고, mylibrary에서 **명시적 상태머신**으로 재설계했다.

### 8.1 구현 개요
- `mobile_scanner` ^7.2.0 기반 ISBN 바코드 스캔 화면.
- ISBN 검색: **NAVER ISBN(`book_adv`) → 실패 시 Google Books(`isbn:`) 폴백** (`book_search_service.searchByISBN`). 텍스트 검색도 네이버→구글 동일 폴백.
- 검색 예외를 타입으로 구분: `BookSearchNetworkException`(연결 실패/타임아웃) / `BookSearchRateLimitException`(HTTP 429) / 기타. UI가 사유별 메시지로 분기.

### 8.2 상태머신 (`_Phase`, 8상태)
`scanning / processing / navigatingToAdd / cardNotFound / cardNetworkError / cardRateLimited / cardCancelled / cardError`
- 상태 전이는 단일 지점(`_to`)에서만 일어난다.
- 바코드 detect는 `scanning` 상태에서만 처리한다(단일 가드 `_phase != scanning`).
- 결과 카드 5종(`card*`)은 실패 사유별 메시지를 표시하고, 그 상태에서는 detect를 무시한다.

### 8.3 핵심 불변식 (회귀 방지 — 깨지 말 것)
1. **카메라 lifecycle은 `mobile_scanner` autoStart에 위임한다.** 수동 `start()`/`stop()` 미사용 (과거 이중 start로 인한 `controllerInitializing` 충돌의 원인). 화면 생존 동안 카메라는 계속 running.
2. **`detectionSpeed.normal` + 앱레벨 dedup(`_lastProcessedIsbn`)으로 중복 차단.** `noDuplicates`는 stop 없이 플랫폼 캐시를 못 비워 "같은 책 재스캔"을 막으므로 금지.
3. **dedup 락 해제(`_lastProcessedIsbn = null`)는 "다시 스캔" 버튼 단일 지점에서만.** 결과 카드 진입 전이와 저장 후 복귀는 모두 락을 유지한다 → 같은 책 자동 재검색·429 재유발 차단.
4. **`MobileScanner` 위젯은 항상 트리에 상주한다.** 어떤 상태에서도 조건부 제거 금지(제거 시 카메라 dispose). 오버레이(결과 카드·처리 표시)는 `Positioned`로 위에 쌓는다.

### 8.4 저장 / 취소 구분
- `add_book_screen`은 저장 시 `context.pop(true)`, 취소(back) 시 `pop(null)`을 반환한다.
- 검색결과 add·직접입력 add 모두 저장→`scanning`(락 유지), 취소→`cardCancelled`(락 유지). 시스템 back은 `PopScope(canPop: true)`로 어떤 상태에서도 스캔 화면을 벗어난다.

### 8.5 표지
- 외부 표지(네이버/구글 https URL)는 Storage 업로드 없이 `cover_url`에 직접 저장 (#11 불변 유지, §7 참조).

### 8.6 미완 / 백로그
1. **[완료]** 표지 미표시 원인 확인 및 해결.
   레거시 bookshelf 앱의 로컬 캐시 경로(`/data/user/0/com.bookshelf.bookshelf/...`)가
   CSV에 포함되어 임포트된 것이 원인. Supabase에서 직접 null 업데이트로 해결.
   재발 방지: `CsvImportService`에서 `http(s)://` 미시작 `cover_url`은 null 처리 추가.
2. **[완료]** flush DNS 실패 시 backoff 재시도 + 포그라운드 복귀 트리거 구현.
   DNS 실패(전체 항목 실패) 감지 시 3초 후 1회 자동 재시도.
   AppLifecycleState.resumed 시 triggerFlush() 호출(_isFlushing 가드로 중복 방지).
   실기기(Galaxy Z Flip 5) 검증 완료 — 오프라인 중 추가한 책 2권이 온라인 복귀 후
   Supabase insert + Storage 표지 promote까지 정상 완료.
3. **누적 고아 표지 정리(운영 과제)**: anon auth 특성상 세션 분리(재설치 등) 시 새 user_id 폴더가 생기고 이전 세션 표지가 Storage에 고아로 남는다(실측: covers에 b27e0ea7-... 등 이전 세션 폴더 잔존). 또한 delete 표지 정리가 best-effort라 복귀 직후 flush 실패분이 누적될 수 있다. linkIdentity 도입 전까지는 books.id에 없는 covers 파일을 주기적으로 식별·정리하는 과제가 남는다(현재는 대시보드 수동 정리로 대응).
4. **[완료]** 패키지명 `com.example.mylibrary` → `com.giltech.mylibrary` 변경 완료.
   `android/app/build.gradle.kts`, `AndroidManifest.xml`, `MainActivity.kt` 경로 변경.
   (2) 릴리즈 keystore SHA-1 등록 + (3) API 키 Android 앱 제한은 릴리즈 빌드 시점에 진행.

### 8.7 수기 입력 fallback (검색 실패 시 ISBN 프리필 등록)

**진단 정정**: 당초 §8.6-2에 "등록 불가"로 적었으나 실측 결과 진입 경로는 이미 존재한다. `_runSearch`의 예외 매핑이 4종 전부 card 상태로 떨어지고(429→`cardRateLimited`, network→`cardNetworkError`, 기타→`cardError`, 결과없음→`cardNotFound`), 5개 card 상태 모두 `_NotFoundCard`("직접 입력" 버튼 포함)를 재사용한다. 실제 마찰은 **검색 실패 시 ISBN을 프리필해 넘기는 경로가 없어, 사용자가 스캔한 ISBN을 수기로 다시 입력**해야 한다는 점이었다.

**설계**:
1. **진입**: 모든 card 상태의 "직접 입력" 버튼 → `_goToManualAdd`. (기존 유지)
2. **ISBN 프리필**: `_goToManualAdd`는 스캔된 ISBN이 있으면 인라인으로 `BookSearchResult(googleId:'', title:'', authors:const[], categories:const[], language:'', isbn13/isbn10: 스캔ISBN)`을 생성해 `/add`의 extra로 넘긴다. ISBN이 없으면 extra 없이 `/add` push(빈 폼). **`.manual` factory는 만들지 않는다** — 빈 껍데기 생성은 파싱 factory(`fromNaver`/`fromJson`)와 의미 층위가 다르고 호출처가 단일하므로, 코어 모델(`BookSearchResult`, §3)에 화면 로직을 들이지 않는다.
3. **저자 필수 유지(§9.1 정렬 보호)**: `add_book_screen`의 제목·저자 `_requiredValidator`는 그대로 둔다. 직접 입력은 ISBN만 프리필, 제목·저자는 수기. 저자 빈 문자열을 허용하면 `bookshelfCompare` 저자순 정렬이 더럽혀지므로(빈 문자열=4순위), acquisition 속도보다 정렬 데이터 품질을 우선한다. 직접 입력은 검색 실패 시의 소수 예외 경로이지 메인 플로우가 아니다.
4. **복귀(불변 §8.3-3 유지)**: 직접 입력은 검색결과 add와 동일한 복귀 규칙을 따른다 — `navigatingToAdd`로 전이(detect 무시) → push 결과로 분기: 저장(`pop(true)`)→`scanning`(락 유지), 취소(`pop(null)`)→`cardCancelled`(락 유지). **dedup 락 해제는 여전히 `_rescan` 단일 지점에서만**. 직접 입력 경로는 `_lastProcessedIsbn`을 건드리지 않는다 → 같은 책 자동 재검색·429 재유발 차단.

**구현 범위**: 실작업은 `_goToManualAdd` 한 메서드(ISBN 유무 분기 + 검색결과 add와 복귀 분기 통일). `add_book_screen`·`_NotFoundCard`·`BookSearchResult`·예외 매핑은 전부 무수정.

**[완료]** `_goToManualAdd`에 스캔 ISBN 프리필 구현 및 실기기 검증 완료.

---

## 8.8 C 작업 진행 상황

### 완료

- **C-1** Supabase 스키마 마이그레이션 — status rental 추가, is_read/medium/language/call_number 컬럼 추가
- **C-2** Drift 스키마 v4 — 동일 4개 컬럼 + migration addColumn 4건
- **C-3** 모델·화면 확장 — `Book` 도메인 모델 필드 추가, `add_book_screen` UI(매체 토글·읽은 책 스위치·청구기호), `book_detail_screen` 상세 표시, `book_list_screen` 상태 뱃지 3-way, `status rental` 전 화면 반영
- **C-4** CSV 내보내기 — CsvExportService, SettingsScreen 신규, /settings 라우트,
  AppBar 설정 아이콘, Android Download 폴더 직접 저장(API 29+, 폴백: 앱 캐시),
  share_plus 미사용(다운로드 직접 저장 + 완료 다이얼로그), iOS는 TODO 주석 보류
  (commit 527a079)
- **C-5** CSV 가져오기 — CsvImportService(인코딩 감지 UTF-8/CP949, 헤더 자동 매핑,
  중복 감지 ISBN 우선→제목+저자, 건너뛰기/덮어쓰기), CsvImportScreen(미리보기·
  체크박스·중복 뱃지·결과 화면), /csv-import 라우트, file_picker ^8.0.0 /
  charset_converter ^2.1.1 추가, 완료 후 목록 직행

### 완료 (추가)
- **C-6** Legacy 구글 시트 실데이터 검증 완료
  - 458권 중 457건 임포트 성공 (1건은 기존 중복으로 스킵)
  - CSV import 누락 필드 보완: language·priorityRead·description·표지URL 키워드 추가 (f57a821)
  - ISBN 지수 표기(9.79E+12) 정규화 추가 — 단, 구글 시트가 숫자 셀로 저장 시 정밀도 손실 발생.
    시트에서 ISBN 컬럼을 텍스트 형식으로 변경 후 재내보내기로 해결.

- **수정 저장 후 상세화면 미갱신 버그 fix** (b94f27e)
  - 원인: `ref.invalidate(booksProvider)` 후 즉시 `context.pop()` 호출 → provider가 AsyncLoading 상태에서 상세화면 복원
  - 수정: `invalidate` → `await ref.read(booksProvider.future)` → `pop` 순서로 변경

- **수정 모드 빈 문자열 필드 삭제 안 되는 버그 fix** (2e6fab9)
  - 원인: `_nonEmpty()` 헬퍼가 빈 문자열 → null 반환 → `copyWith(null)` → 기존 값 유지
  - 수정: 수정 모드 `copyWith` 호출부에서 `_nonEmpty()` 제거, 빈 문자열 직접 전달. `review`는 빈 문자열 허용(삭제 반영), 나머지 옵션 필드는 빈 문자열이면 null

- **상세화면 필드 순서 변경 + 수정화면 읽은책 토글 우측정렬** (2987597)
  - 상세화면: 출판연도·출판사·책장 위치·청구기호·ISBN·매체·장르·총 페이지 순서 확정
  - 수정화면: `Row(mainAxisAlignment: MainAxisAlignment.end)` 적용으로 토글 우측 정렬

- **집계 탭 폐기 결정**: 별도 집계 탭 계획 → 통계 탭(§10)으로 대체. `stats_screen`이 단일 진입점.

### "나의 도서관" 명칭 확정

앱 내 서재 공간의 공식 명칭을 **"나의 도서관"**으로 확정한다.

**철학적 배경**: 나심 탈레브의 안티라이브러리(antilibrary) 개념에 기반한다. 읽지 않은 책은 결핍이 아니라 가능성이다. 미독을 부정적으로 표현하는 문구는 앱 어디에도 사용하지 않는다. 통계 화면(§10)의 동적 문구가 이 철학을 전면에 드러낸다.

- UI 표시: "내 서재" (기존 AppBar 레이블 유지, 내부 철학 용어와 구분)
- 코드/문서: "나의 도서관" 또는 "my library"
- 반드시 피할 표현: "읽지 않은 책이 N권 있습니다", "아직 N권을 못 읽었습니다" 등 미독을 결핍으로 묘사하는 모든 문구

### import 설계 확정 (C-5)

| 항목 | 결정 |
|---|---|
| 중복 감지 기준 | ISBN 일치 우선 → 없으면 제목+저자 동일 |
| 미리보기 | 컬럼 매핑 확인 후 행 단위 체크박스로 선택 import |
| 인코딩 | UTF-8 기본, CP949(EUC-KR) 자동 감지 후 변환 |
| 컬럼 매핑 | 헤더 키워드 자동 인식(제목/title, 저자/author 등), 미인식 시 드롭다운 수동 지정 |
| 중복 처리 | 사용자 선택(건너뛰기/덮어쓰기) |

---

## 9. 제거 / 폐기 대상 (기존 코드)

- `services/sheets_service.dart` (시트 동기화)
- `apps_script/Code.gs`, `apps_script/cover_upload.gs`
- 302 리다이렉트 수동 추적, `text/plain` CORS 우회 로직
- `cover_upload_service.dart`의 base64→Apps Script→Drive→lh3 사슬
- `services/database_service.dart` (sqflite) → Drift로 대체
- `provider` 기반 상태관리 → riverpod
- SharedPreferences 키 `sheets_webhook_url`, `sheets_sync_enabled`
- Google Sheets는 **선택적 내보내기(export-only)로만** 잔존 가능 (1차 저장소 아님)

---

---

## 10. 통계 화면 (stats_screen)

### 구조: 통계 홈 + 드릴다운 4개

통계 탭(`/stats`)은 **홈 화면** + **드릴다운 화면 4개**로 구성된다.

```
/stats          → StatsScreen       (통계 홈, 하단 탭 3번째)
/stats/year     → StatsYearScreen   (출판연도 분포)
/stats/author   → StatsAuthorScreen (가장 많이 만난 저자)
/stats/monthly  → StatsMonthlyScreen(월별 책과의 만남)
/stats/wishlist → StatsWishlistScreen(오래 기다린 책)
```

드릴다운은 `/stats` 하위 GoRoute로 정의 → 하단 탭 유지, 뒤로가기로 홈 복귀.

---

### 통계 홈 (`/stats`)

한 화면 안에 들어오는 구성. `ListView` (항목 고정, 스크롤 허용).

| 항목 | 위젯 | 조건 |
|------|------|------|
| 섹션 1 | 안티라이브러리 대시보드 | 항상 |
| 섹션 2 | 매체별 분포 + 언어별(조건부) | 항상 |
| 드릴다운 카드 | 출판연도 분포 | 항상 |
| 드릴다운 카드 | 가장 많이 만난 저자 | 항상 |
| 드릴다운 카드 | 월별 책과의 만남 | 항상 |
| 드릴다운 카드 | 오래 기다린 책 | wishlist 책 있을 때만 |

드릴다운 카드: 제목 + 미리보기 텍스트 + chevron_right → `context.push(route)`.

**미리보기 텍스트 로직:**

- **출판연도**: 최빈 버킷(미상 제외) → `"2010년대 책이 가장 많습니다"` / 연도 정보 없으면 `"연도 정보가 없습니다"`
- **저자**: 모든 저자가 1권이면 `"다양한 저자의 책을 소장하고 있습니다"` / 아니면 `"TOP저자 외 N명의 저자"`
- **월별**: tracking 미설정 → `"지금부터 기록을 시작할 수 있습니다"` / 설정 시 → `"X월부터 기록 중 · 총 N권"`
- **희망**: 가장 오래 기다린 책 제목 + `"N일째 기다리는 중"`

---

### 섹션 1 — 안티라이브러리 대시보드

- 전체 N권, 읽음/미독 progress bar
- 소장 / 희망 / 대여 / 우선읽기 카운트 칩 (4개 Expanded)
- 미독 비율 동적 문구:
  - 80%+: "당신의 도서관은 무한한 가능성을 담고 있습니다"
  - 50–79%: "당신의 도서관에는 N권의 가능성이 있습니다"
  - 30–49%: "도서관의 절반이 아직 당신을 기다립니다"
  - 30% 미만: "깊이 읽어내는 도서관입니다"
- **불변**: 미독을 결핍으로 표현하는 문구 절대 금지

---

### 섹션 2 — 매체별 + 언어별

- 종이책/전자책/오디오북 3칸 카드 (권수 + %)
- 언어 행(조건부): language 필드 20% 이상 + 'ko' 단일이 아닐 때만 표시.
  상위 4개 언어 + 기타 집계. 텍스트 형식: `"한국어 300 · 영어 60 · 기타 5"`

---

### 드릴다운 — 출판연도 분포 (`/stats/year`)

**1년 단위** 가로 막대. 0권 연도도 표시(dim 색, 탭 비활성). 최댓값 기준 상대 비율.

범위: `books 중 year 최솟값 ~ max(현재연도, year 최댓값)` 내림차순. 미상(year null/빈) 맨 끝.
상단 설명: `"YYYY년부터 올해까지"` + Divider (year 데이터 있을 때만).
행 탭 → DraggableScrollableSheet (0.5/0.3/0.9) 해당 연도 책 목록.

---

### 드릴다운 — 가장 많이 만난 저자 (`/stats/author`)

**2권 이상** 소장 저자만 필터. 권수 내림차순 + 동점 가나다순 → **TOP 30** 가로 막대.
30명 초과 시 맨 끝에 "외 N명" 행(탭 없음).
2권 이상 저자 없으면 "모든 저자의 책을 1권씩 소장하고 있습니다" (목록·구분선 미표시).
상단 설명: `"2권 이상 같은 저자인 경우"` + Divider.
저자 행 탭 → DraggableScrollableSheet (0.5/0.3/0.9) 해당 저자의 책 목록.

---

### 드릴다운 — 월별 책과의 만남 (`/stats/monthly`)

- `effectiveDate = acquired_at ?? created_at`
- `tracking_started_at` 이후 책만 집계 → 최근 12개월 가로 막대
- 미설정: "지금부터 나의 도서관에 새로 들어오는 책의 흐름을 기록합니다" + [오늘부터 시작] 버튼
  → `profiles.tracking_started_at = today` 저장 (ProfileRepository.setTrackingStartedAt)

---

### 드릴다운 — 오래 기다린 책 (`/stats/wishlist`)

status = 'wishlist' TOP 20. 경과일 = 오늘 − created_at. 탭 → `/books/:id`.

---

### 공통 위젯 (`stats_common.dart`)

드릴다운 화면 공유 위젯 + 함수.

| 항목 | 설명 |
|------|------|
| `StatsBarRow` | 라벨 90px + 가로 막대 Expanded + 권수 32px |
| `StatsSectionCard` | 패딩 16, borderRadius 12, dim 0.5px 테두리 |
| `StatsSectionTitle` | muted 12sp w600 letterSpacing 0.5 |
| `StatsSmallCover` | 36×50 px 책 표지 썸네일 |
| `yearBucket()` | String? → 5년 버킷 문자열 |
| `yearBucketOrder` | 버킷 정렬 순서 상수 리스트 |
| `langNames` | 언어 코드 → 한국어 표기 맵 |

---

### 구현 제약

- 차트: `Container` 비율 막대. **외부 패키지 금지** (fl_chart 등).
- 팔레트: 기존 다크 골드 팔레트 준수 (AppColors.*).
- 데이터: `booksProvider`(Drift 로컬) + `trackingStartedProvider`(Supabase profiles).

---

---

## 11. 홈 화면 (`/home`)

### 탭 구조 (결정 #16)

```
하단 탭 4개: [홈] [서가] [통계] [설정]
  index 0 → /home        HomeScreen
  index 1 → /            BookListScreen
  index 2 → /stats       StatsScreen (+ 하위 드릴다운)
  index 3 → /settings    SettingsScreen
initialLocation: '/home'
```

### 화면 구조

```
Scaffold(body: Stack(fit: expand))
  ├─ 레이어1: 배경 — Image.file(home_bg.webp) 또는 ColoredBox(AppColors.bg)
  ├─ 레이어2: LinearGradient 오버레이 (투명→0xCC→불투명, stops 0/0.5/1.0)
  └─ 레이어3: SafeArea → SingleChildScrollView → Column
       ├─ 헤더 (도서관 이름 + 편집 아이콘 + "N권 · D일째")
       ├─ 카드1: 오늘의 책 (미독 소장본 날짜 시드 랜덤)
       ├─ 카드2: 요약 (읽음/미독 권수 + 가장 많이 만난 저자)
       └─ 카드3: 가장 오래 기다린 희망도서 (선택적)
```

### 도서관 이름 (library_name)

- `profiles.library_name TEXT` — Supabase upsert
- 미설정: 헤더에 "나의 도서관" (muted 15sp)
- 설정 시: 소제목 "나의 도서관"(muted 12sp) + 큰 이름 (cream 22sp bold)
- 편집: 연필 아이콘 탭 → AlertDialog + TextField. 빈 값 저장 시 null 처리.
- 상태: `LibraryNameNotifier extends AsyncNotifier<String?>` in providers.dart

### 오늘의 책 (카드1)

- 대상: `!isRead && status == 'owned'` 목록
- 랜덤 시드: `year * 10000 + month * 100 + day` (날짜 고정, 매일 바뀜)
- 미독 소장본 없으면 카드 미표시

### 나의 도서관 요약 (카드2)

- 읽음 N권 / 미독 N권 (Row 양끝)
- 가장 많이 만난 저자: 2권 이상 저자 中 최다 → 1명 표시. 없으면 미표시.

### 가장 오래 기다린 희망도서 (카드3)

- `status == 'wishlist'` 중 `createdAt` 기준 가장 오래된 책
- "N일째 기다리는 중" (today − createdAt + 1)
- 희망도서 없으면 카드 미표시
- 탭 → `/books/:id`

### D일째 계산

- `books` 전체 `createdAt` 최솟값 기준
- `today.difference(earliest).inDays + 1`
- `createdAt` 있는 책이 없으면 미표시

### 배경 이미지 (HomeBackgroundNotifier)

위치: `lib/features/home/home_background_notifier.dart`

- 저장 경로: `getApplicationDocumentsDirectory()/home_bg.webp`
- `pickAndSave()`: `ImagePicker` 갤러리 → `FlutterImageCompress.compressWithFile` webp, 80→60→40 순, 500KB 이내 시 저장
- `remove()`: 파일 삭제 + state null
- 설정 화면: `[홈 배경 이미지]` 섹션 — 100×60 썸네일 + [이미지 변경] + [배경 제거](이미지 있을 때)

---

_갱신 규칙: 결정 변경 시 이 문서를 먼저 고치고 커밋한다. 코드와 문서가 어긋나면 문서가 기준이며, 어긋난 코드를 문서에 맞춘다._
