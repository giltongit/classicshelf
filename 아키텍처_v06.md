# 클래식 음반 컬렉션 앱 — 아키텍처 v06

> **본 문서는 `아키텍처_v05.md`를 대체한다.** 이후 개발은 본 문서만 참조한다.
> 플랫폼: Flutter (iOS 우선 → Android) / Backend: Supabase / Local DB: Drift (SQLite)
> 개발 형태: 솔로 개발, 상업 출시 목표

> **v06의 성격**: v05가 "코어 컬렉션 루프가 실데이터로 닫힌 상태"였다면, v06은 **대 1의 남은 두 축(소장/희망 UI, 필터 시트 UI)이 추가로 닫힌 상태**를 반영한다.
> 이번 세션에서 위시리스트 UI(자유 텍스트 기반 등록·조회·삭제, 확정 연결·자동 해소는 잔여)와 필터 시트 UI(status/format/composer/period/conductor/onlyNeedsVerification 5축 전부 연결, 헤드리스+실기 검증)가 구현됐다.
> **주의**: `아키텍처_v05.md`는 저장소에 커밋된 적이 없다(직전까지 `v04.md`만 있었음). 이번 커밋으로 `v04.md`를 제거하고 `v06.md`만 남긴다 — v05는 저장소 관점에선 존재하지 않았던 셈이다.

---

## 0. 문서 위상

| 문서 | 상태 |
|---|---|
| `아키텍처_v1/v02/v03/v04/v05.md` · `FEATURES.md` · `공통_아키텍처.md` | 폐기 |
| `아키텍처_v06.md` | 현행 단일 기준 |

**v06의 핵심 변경 (v05 대비)**
- **위시리스트 UI 구현**: 목록(reactive) · 추가(자유 텍스트: composer 필수·title 선택) · 삭제(스와이프+길게눌러 확인).
- **위시리스트 스키마 변경**: `wishlist.composer`/`wishlist.title` 자유 텍스트 컬럼 추가(`compositions.title` 선례 계승), `wishlist_target_ck` 완화(FK 없어도 자유 텍스트로 유효, 단 type-FK 상호배제는 유지).
- **Drift schemaVersion 2 → 3**: wishlist composer/title 컬럼, `onUpgrade` addColumn만 사용(drop 없음), 실기 무손실 검증.
- **필터 시트 UI 구현**: status/format/composer/conductor/onlyNeedsVerification **5축**(period는 제외 — §17-22), facet(작곡가·지휘자 distinct — 지휘자는 앨범기본∪곡별override) · 조합 필터(교집합) · 검색어 동시 적용 · 활성 필터 뱃지 · 초기화(검색어·정렬 보존) · 취소(무반영). 헤드리스 검증 5건 통과, 실기 탭 검증은 미확인.
- **회귀 테스트 추가**: `test/album_filter_test.dart` (필터 조합·facet 범위 회귀 방지, `save_album_replace_children_test.dart`와 같은 성격).
- **총 12 테이블 유지** (wishlist는 컬럼만 추가, 테이블 수 변화 없음).

---

## 1. 제품 정의 (변경 없음)

### 1-1. 철학

| 앱이 하는 것 | 앱이 하지 않는 것 |
|---|---|
| 소장·희망 음반 정리 | 음원 스트리밍 |
| AI 음악 해설 | 음반 판매·시세 정보 |
| 악보 탐색 | 팝·록 등 비클래식 장르 |
| 클래식 특화 검색 | YouTube API를 통한 인앱 재생 |

**핵심 가치**: 클래식 음반 수집과 감상 문화의 고차원화. 스트리밍 대체재가 아니다.

### 1-2. v1.0 범위

**코어**: 음반 등록(자동입력+수동입력) · 소장/희망 관리(음반+작품 단위) · 클래식 특화 계층 조회·검색
**보조**: AI 음악 해설 · 유튜브 특화 검색 · 컬렉션 통계 · 관리자 편성 큐레이션

### 1-3. 제외 및 이관

| 항목 | 판단 | 근거 |
|---|---|---|
| 커뮤니티 | v1.1 이후 | Apple 1.2 UGC 의무(EULA·필터링·신고·차단·24시간 대응)가 솔로 개발자에게 영구 부담 |
| 공연 일정 | KOPIS 연동 이후 | 관리자 수동 편성 지속 불가 |
| 악보 | 조건부 별도 트랙 | 법적 리스크 최대 (§13) |
| 음반 시세·가격 · 비클래식 | 영구 제외 | 철학 위배 |

---

## 2. 기술 스택

| 영역 | 선택 | 상태 |
|---|---|---|
| Frontend (앱) | Flutter (Dart) | ✅ |
| Frontend (웹 콘솔) | React + Supabase JS | 미착수 (대 3.5) |
| 상태관리 | Riverpod | ✅ |
| 라우팅 | go_router | ✅ (`/wishlist` 브랜치 추가) |
| Backend | Supabase (PostgreSQL, Edge Functions, RLS, Storage) | 스키마·RLS·인증 ✅ / Edge Functions 미착수 |
| Local DB | Drift 2.34.x + drift_flutter | ✅ (**schemaVersion 3**, onUpgrade 2단계 검증: v1→v2 title, v2→v3 wishlist composer/title) |
| 로컬 DB 암호화 | SQLCipher (`sqlcipher_flutter_libs`) | **미적용** (§17, 배선만 가능) |
| 로컬 검색 | SQLite FTS5 (trigram) | **미구현** — 현재 LIKE 폴백 (§6-3) |
| 결제 | RevenueCat | 미착수 (대 4) |
| AI | Claude API / Vision API | 미착수 (대 3) |
| 참조 데이터 시드 | Open Opus 덤프 | 미착수 (대 1-A 잔여) |
| 바코드 | `mobile_scanner` | 미착수 (대 2, 의존성만 존재) |
| 외부 이동 | `url_launcher` | 미착수 (대 2/§8) |
| 인증 | Supabase Auth (익명·Email·Google·Apple) | 익명 로그인 실가동 ✅ (Email/Google/Apple 미배선) |
| 앱 식별자 | `com.giltech.classicshelf` (Android) | ✅ / **iOS·macOS·Linux는 `com.example.mylibrary` — 변경 필요(§17)** |

**Supabase 프로젝트 (정본)**: `bxjjychftwuaipmxsqyo` (이름: **classicalshelf**, PRODUCTION).
- 저장소·앱ID는 `classicshelf`, 프로젝트명은 `classicalshelf` — 한 글자 차이(-al-) 혼동 주의.
- 폐기 대상: `uzqqmimqefzynbyrwnom`(구 도서앱 자산, git 이력에 URL+anon key 노출 — publishable key라 저위험).

---

## 3. 데이터 모델 ★

### 3-0. 식별자 전략 (변경 없음)

**클라이언트 생성 UUID 단일 키.** 오프라인에서 앨범 생성 시 자식이 부모 UUID를 즉시 참조 → 재매핑 불필요. 편집 시 id 보존(`.existing` 생성자 패턴), 신규만 `uuid.v4()`. 위시 항목도 동일 원칙(항상 신규 발급, 편집 개념 없음).

### 3-1. 계층

```
Works (정규 작품 · 참조 데이터)
 ├─ 작곡가 · 작품명 · 작품번호 · 조성 · 장르 · 시대 · popular/recommended · source
 ├─ WorkMovements (표준 악장 구성)
 └─ WorkAliases (표기 변형)

Album (음반 · 애그리게이트 루트)
 ├─ 기본 연주자 (AlbumPerformers)
 └─ Composition (수록곡)
     ├─ title (자유 텍스트 작품 제목)
     ├─ → Works 참조 (workId, nullable — 미매칭 허용)
     ├─ composer · catalogNumber · 디스크 번호 · 트랙 범위 · confidence
     ├─ 연주자 예외 (CompositionPerformers) → 없으면 앨범 기본값 상속
     └─ Movement (실제 수록 악장)

Wishlist (독립 애그리게이트 루트)
 ├─ type (album | work)
 ├─ albumId / workId (확정 연결, nullable — 현재 대부분 null)
 ├─ composer / title (자유 텍스트, v06 신규 — compositions.title 선례 계승)
 └─ priority / note

Commentaries (AI 해설 캐시, 공용)
Profiles (도서관명·추적 시작일)
SyncQueue (로컬 고유 — 오프라인 동기화 큐)
```

**총 12 테이블** (참조 4 + 사용자 6 + profiles 1 + 큐 1) — 테이블 수는 v05와 동일, wishlist는 컬럼만 추가.
- `20260725052147_init_classical_schema.sql` (11테이블, RLS·트리거)
- `20260726231758_profiles.sql` (profiles + 소유자 RLS + updated_at 트리거)
- `20260727080019_composition_title.sql` (compositions.title 컬럼)
- `20260804120000_wishlist_free_text.sql` (wishlist.composer/title 컬럼 + `wishlist_target_ck` 완화, **v06 신규**)

### 3-1a. 작품 제목(title) 필드 (변경 없음)

Composition에 자유 텍스트 `title`(nullable). title과 workId 공존. 표시 우선순위: title 있으면 주 표시. 선택 입력(composer·catalogNumber·title·movements 전부 비면 빈 카드 제외).

### 3-1b. 위시리스트 자유 텍스트 필드 (v06 신규) ★

`compositions.title`과 같은 문제(참조 데이터 없이도 사람이 읽는 이름을 저장해야 함)를 wishlist에도 동일 해법으로 풀었다.

- **배경**: `wishlist_target_ck`가 원래 `type=album→album_id 필수`, `type=work→work_id 필수`로 강제했다. Works 시드(대 1-A) 미착수·albums는 소장 컬렉션이라 위시(아직 안 가진 것) 대상이 없어 **자유 텍스트로 위시를 표현할 방법이 없었다** — 필터 시트 이전에 먼저 걸린 구조적 문제.
- **해법**: `composer`(text)·`title`(text) 컬럼 추가. `wishlist_target_ck`를 "FK 없어도 composer/title 중 하나라도 있으면 유효"로 완화하되, **type과 FK의 상호배제는 유지**(`type=album`이면 `work_id`는 반드시 null, 반대도 동일) — 향후 확정 연결 작업에서 유형·FK 불일치 행이 생기는 것만 구조적으로 막는다.
- **albumId/workId는 남겨둠**: 지금은 폼에서 채우지 않지만(확정 연결 UI 없음), Works 시드 이후 매칭이나 자동 해소 감지가 붙을 자리로 스키마에 유지.
- `isValid`: composer·title·albumId·workId 중 하나라도 있으면 유효 — compositions의 "하나라도 있으면 유효" 규칙과 동일 정신.

### 3-2. 애그리게이트 경계 (변경 없음)

- **Album이 애그리게이트 루트.** Composition·Movement·Performer는 그 내부.
- **Wishlist는 독립 루트** — 항목 단위 insert/delete(`saveWishItem`/`deleteWishItem`)면 충분. Album의 LWW 통째 덮어쓰기 함정은 wishlist엔 해당 없음(폼에 없는 필드가 유실될 애그리게이트 구조가 아님).
- **LWW 통째 덮어쓰기 함정** ★ (Album 한정, 변경 없음): 편집 시 폼에 입력란 없는 필드(coverUrl·barcode·disposedAt)도 pre-fill 값을 실어야 유실 안 됨.

### 3-3. 연주자 상속 ★ (변경 없음, 필터 facet에서 재확인)

연주자는 Composition에 귀속. role 단위 병합, null(상속)과 override 구분. **v06에서 재확인**: 필터의 지휘자 facet(distinct 조회)이 앨범 기본값과 곡별 override 양쪽을 UNION으로 수집하도록 구현·검증됨 — 상속 규칙이 조회 경로(리포지토리)에서도 일관되게 지켜지고 있음을 필터 헤드리스 테스트가 재확인.

### 3-4. WorkMovements vs Movements (변경 없음)

### 3-5. 신뢰도 상태 (변경 없음)

### 3-6. 캐시 vs 로컬 고유 상태

| 구분 | 대상 | 스키마 변경 시 |
|---|---|---|
| 캐시 | Works/…/Commentaries, Albums/…/Wishlist, Profiles | drop & 재동기화 가능 |
| 로컬 고유 | **SyncQueue** (미전송 편집) | 정식 마이그레이션 필수 |

- **스키마 진화는 onUpgrade** (drop-재생성 아님). **v06 갱신**: v1→v2(title, `m.addColumn`) · **v2→v3(wishlist composer/title, `m.addColumn` ×2, v06 신규)** — 둘 다 실기 무손실 검증됨.

### 3-7. FK 강제 비대칭 (변경 없음)

### 3-8. 소유권 검증 (변경 없음)

- **wishlist는 트리거 누락** (저위험) — 유지, 이번 작업 범위 밖.

### 3-9. Works 참조 데이터 출처 (변경 없음, 대 1-A 미착수)

---

## 4. 음반 등록 ★ (변경 없음 — 이번 세션은 등록 폼을 건드리지 않음)

### 4-1~4-8

v05와 동일. 수동 등록 폼 실가동, 자동입력 4수단은 대 2 미구현. 표 전환·세트 일괄·악장 순서 조정 미구현(§17).

---

## 5. 메타데이터 파이프라인 (미착수, 대 2, 변경 없음)

---

## 6. 컬렉션 관리 ★ (소장/희망·필터 UI 구현 완료)

### 6-1. 목록 · 상세 (실검증)

- **목록**: `AlbumSummary` 경량 뷰. 카드(커버·title·primaryComposer·format·releaseYear·수록곡수·확인필요/처분 배지). reactive. 탭 → `/albums/:id`. 정렬·검색 연결. **필터 시트 UI 구현·실검증 완료 (v06)** — 이전 스텁(스낵바) 제거.
- **상세**: `Album` 애그리게이트 통째 조립. 3단 계층, 작품 제목 주 표시, 연주자 상속 방식 2, 편집 진입, 삭제.

### 6-2. 소장 / 희망 ★ (v06 — UI 구현 완료)

- **소장**: 기존 앨범 목록·상세가 커버(HoldingStatus owned/disposed). 이번 세션에서 손대지 않음.
- **희망(위시리스트)**: `/wishlist` 탭 신설. 목록(reactive, `watchWishlist`, 등록순) · 추가(바텀시트, composer 필수·title 선택·type 라디오) · 삭제(스와이프+길게눌러 확인). 자유 텍스트 기반(§3-1b) — Works 시드 없어 자동완성 없음.
- **미구현으로 남음**: 위시 항목 → 실제 album/work **확정 연결**(대 1-A 이후), **자동 해소 감지**(음반 등록 시 매칭 위시 자동 표시/삭제 — 설계만 있고 구현 안 됨, §17 부채 20·21).

### 6-3. 검색 · 필터 ★ (v06 — 필터 UI 구현 완료)

- **필터 시트**: status/format/composer/conductor/onlyNeedsVerification **5축** 연결(period 제외 — 아래). facet(작곡가·지휘자 distinct, 지휘자는 앨범기본∪곡별override) · 조합 필터(교집합, 합집합 아님) · 검색어와 동시 적용 · 활성 필터 개수 뱃지 · 초기화(검색어·정렬 보존) · 취소(무반영).
  - **검증 상태**: 헤드리스 5건 통과(facet·축별 단독·조합 교집합·검색어 동시·초기화). `flutter analyze` 에러 0. 실기 실행에서 예외·크래시 없음(로그 확인). **화면 탭 검증은 미확인** — §19 참조.
  - composer·conductor는 **자유 텍스트가 아니라 드롭다운**이다. 리포지토리 필터가 정확 일치(`==`/`.equals()`) 매칭이라 자유 텍스트로 받으면 한 글자만 달라도 0건이 된다. 등록된 값에서 뽑은 distinct 목록(`getFilterFacets`)에서 고르게 한다. Works 시드 이후 자동완성으로 대체 가능.
  - **초기화는 시트가 다루는 축만 해제**하고 검색어·정렬은 보존한다(`AlbumFilter.clearedForSheet()`). `AlbumFilter.empty`로 통째 되돌리면 검색창 `TextEditingController`의 글자는 남는데 필터만 풀려 화면과 상태가 어긋난다.
  - 포맷 선택지는 등록 폼과 `kAlbumFormats`(album.dart)로 공유한다 — 두 곳이 다른 목록을 보면 필터가 입력값을 놓친다.
- **period 축은 시트에서 의도적으로 제외** (§17-22): `AlbumFilter.period`는 enum도 연도 범위도 아닌 **자유 텍스트 String**이고, 리포지토리 쿼리는 `compositions.workId → works.period` 조인으로 **이미 구현돼 있다**. 그런데 `works` 테이블에 **쓰는 경로가 코드 전체에 없어**(Open Opus 시드 미착수, 대 1-A) 항상 비어 있다 → 어떤 값을 넣어도 결과 0건. 사용자가 고를 수 있는데 항상 빈 목록이 나오는 죽은 컨트롤이 되므로 노출하지 않았다. **모델 필드와 쿼리는 그대로 두었으므로 시드 후 칩만 붙이면 된다.**
- **FTS5 trigram + 2글자 이하 LIKE 폴백** 설계는 v05와 동일 — **여전히 LIKE 폴백만, FTS5 미생성**(§17 잔여 부채, 별도 작업).
- 원어(라틴) 입력 경향: trigram이 양쪽 문자 체계. 초성 검색은 한국어 필드만.

### 6-4. 통계 (전면 미구현, 변경 없음)

---

## 7~9. (변경 없음)

AI 음악 해설(대 3) · 유튜브 특화 검색(대 2/5) · 악보(대 6) — v05와 동일.

---

## 10. 오프라인 & 동기화 ★

### 10-1. 오프라인 지원 (변경 없음)

### 10-2. 동기화 엔진 ★

v05와 대부분 동일(write·flush·sync·reconcile 전부 구현). **v06 확인 사항**: `saveWishItem`/`deleteWishItem`이 write→flush 공용 경로(`_enqueueRaw('wishlist', …)`, 제네릭 `_supabase.from(item.entityTable)` flush)를 그대로 타므로 **wishlist 전용 배선이 필요 없었음** — 기존 sync 엔진이 이미 wishlist를 커버하고 있었다(Album write 경로와 별개로 신규 구현한 부분 없음).

- **오프라인 큐 왕복 미검증** ⚠️ (변경 없음): 비행기모드 등록→복귀 검증 필요.

### 10-3. 세 표현 경계 (변경 없음)

---

## 11~15. (변경 없음)

유료화(대 4) · 보안 요구사항(§12, 변경 없음 — wishlist 소유권 트리거 누락은 그대로 저위험 부채) · 법적 리스크(§13) · 관리자(대 5) · 계정&컴플라이언스(대 7).

---

## 16. 개발 단계 (진행 현황) ★

```
대 0. 기반                    ✅ 완료
대 1. 코어 컬렉션 루프         🔨 진행 중
  1-A 데이터 모델             ✅ (title 추가·v2, wishlist composer/title 추가·v3, Works 시드만 잔여)
  1-B 등록 폼                 ✅ 신규·편집·악장·제목·override 실가동
                                (표 전환·세트 일괄·악장 순서는 미구현)
  1-C 목록·상세·삭제          ✅
  1-D 소장/희망               ✅ v06 — 위시리스트 UI(목록·추가·삭제) 구현·실검증
                                (확정 연결·자동 해소는 잔여, §17)
  1-E 검색·필터               ✅ v06 — 필터 시트 5축 구현·헤드리스 검증 5건
                                (실기 탭 검증 미확인 / period 미노출 §17-22
                                 / FTS5 trigram 잔여, LIKE 폴백 유지)
  1-F 동기화                  ✅ write·flush·sync·reconcile (오프라인 왕복 미검증)
  1-G 검증 큐·보류함           ❌
  1-H CSV                     ❌
대 2. 입력 자동화             ❌
대 3. AI 해설 + 서버측 강제    ❌
대 3.5 웹 콘솔                ❌
대 4. 유료화                  ❌
대 5. 콘텐츠 + 관리자          ❌
대 6. 악보                    ❌
대 7. 출시 준비               ❌
대 8. 출시 후 → Android       ❌
```

**대 1 남은 축**: Works 시드(1-A), 검증 큐·보류함(1-G), CSV(1-H), 등록 폼 편의(표 전환·세트·악장 순서), 위시 확정 연결·자동 해소, FTS5 trigram. **1-D·1-E는 v06으로 완료 처리.**

### 순서 원칙 (변경 없음)

---

## 17. 미결정 · 부채 사항

| # | 항목 | 상태 |
|---|---|---|
| 1 | MusicBrainz 필드별 라이선스 | 대 2 착수 전 필수 |
| 2 | Vision 실단가 기반 한도 검증 | 대 2 착수 전 |
| 3 | Open Opus 덤프 갱신 주기 | 대 1-A |
| 4 | 로컬 Works 세트 범위 | 대 1-A |
| 5 | CSV 스키마(Discogs 매핑) | 대 1-H |
| 6 | 웹 콘솔 인증 방식 | 대 3.5 |
| 7 | 작품 단위 희망 추천 연계 | v1.1 이후 |
| 8 | SQLCipher 실적용·성능 실측 | 배선 가능, 미적용 |
| 9 | 하위 직접 수정 경로의 watch 갱신 | 검증 큐 구현 시 |
| 10 | 오프라인 큐 왕복 검증 | 비행기모드 등록→복귀 flush (미실증) |
| 11 | 연주자 role 단위 suppress | 필요 확인 시 §3-3 재설계 |
| 12 | 악장 순서 조정 | 위/아래 이동 버튼 미구현 |
| 13 | commentaries anon 제외 | 대 3 |
| 14 | 익명→정식 계정 전환 | 대 7 |
| 15 | iOS/macOS/Linux 번들ID | `com.example.mylibrary` → 변경 |
| 16 | 인코딩 정리 | 저우선 |
| 17 | Supabase 작업(PowerShell) ↔ Code 세션 분리 | db push 후 "적용 완료" 공유 필수 |
| 18 | PGRST303 JWT future | 재발 시 기기 시계 동기화 확인 |
| 19 | `_parseChildrenSnapshot` 빈 리스트 | null 규칙과 어긋남(동작 무해) |
| 20 | **위시 → 실제 album/work 확정 연결** (v06 신규) | 자유 텍스트만 가능. Works 시드(대 1-A) 이후 매칭 UI 또는 앨범 등록 시 "이 위시와 연결" 액션 |
| 21 | **위시리스트 자동 해소 감지** (v06 신규) | 설계만 있고 미구현(§6-2). 음반 등록 시 매칭 위시 항목을 표시/삭제하는 로직 없음 |
| 22 | **필터 — period(시대) 축 미노출** (v06 신규) | 모델(`AlbumFilter.period`, 자유 텍스트 String)·리포지토리 쿼리(`works.period` 조인) 모두 **구현 완료**. 다만 `works`에 쓰는 경로가 없어(대 1-A 미착수) 테이블이 비어 항상 0건 → 시트에서 제외. **Works 시드 후 칩만 추가하면 동작한다**(코드 변경 불필요) |
| 23 | FTS5 trigram 미생성 | LIKE 폴백 유지 중, 별도 작업 필요 |

---

## 18. 현재 코드 구조 (실제 파일 지도)

```
lib/
├── models/
│   ├── album.dart
│   ├── album_summary.dart
│   ├── work.dart                    (미배선 — 대 2)
│   ├── wishlist_entry.dart          WishItem+WishType (composer/title/createdAt 추가, v06)
│   ├── commentary.dart              (미배선 — 대 3)
│   ├── album_filter.dart            필터+정렬 (필터 시트 UI로 실사용, v06)
│   └── model_utils.dart
├── database/
│   └── app_database.dart(.g)        Drift 12테이블 미러 (**schemaVersion 3**, v06)
├── repositories/
│   ├── collection_repository.dart       인터페이스 (watchWishlist 추가, v06)
│   ├── collection_repository_impl.dart  write/flush/read/sync/reconcile
│   │                                     + _wishlistQuery/_wishFromRow(v06)
│   └── profile_repository(_impl).dart
├── providers/
│   ├── providers.dart               + wishlistProvider(StreamProvider, v06)
│   ├── sync_queue_flusher.dart
│   └── cover_upload_provider.dart
├── screens/
│   ├── home_screen.dart
│   ├── book_list_screen.dart        AlbumListScreen — 필터 진입 버튼+활성개수 뱃지(v06, 스텁 제거)
│   ├── album_filter_sheet.dart      신규(v06) — 필터 바텀시트(5축·facet 드롭다운·적용/초기화)
│   ├── book_detail_screen.dart      AlbumDetailScreen
│   ├── add_book_screen.dart         AddAlbumScreen (포맷 목록을 kAlbumFormats로 공유, v06)
│   ├── wishlist_screen.dart         신규(v06) — 목록·빈상태·카드·삭제·추가 바텀시트
│   ├── main_scaffold.dart           탭 4개(홈·서가·희망·설정, v06)
│   └── settings_screen.dart
├── services/
│   └── cover_photo_service.dart     미배선
└── router/app_router.dart           /home·/·/wishlist(v06)·/settings·/add[?albumId]·/albums/:id

test/
├── save_album_replace_children_test.dart
└── album_filter_test.dart           신규(v06) — 필터 조합·facet 회귀 방지

supabase/migrations/
  20260725052147_init_classical_schema.sql
  20260726231758_profiles.sql
  20260727080019_composition_title.sql
  20260804120000_wishlist_free_text.sql   신규(v06)
```

**필터 시트 분리 (v06 확정)**: 별도 파일 `lib/screens/album_filter_sheet.dart`. `book_list_screen.dart`에는 진입 버튼(`_FilterButton` — 활성 축 개수 뱃지)만 남겼다. 시트는 `showAlbumFilterSheet(context)` 하나로 열린다.

**v06에서 함께 추가된 것** (위 지도에 안 보이는 심볼):
- `album.dart` — `kAlbumFormats` (등록 폼·필터 공유 포맷 목록)
- `album_filter.dart` — `activeCount`(뱃지용), `clearedForSheet()`(검색어·정렬 보존 초기화)
- `collection_repository.dart` — `FilterFacets` 타입 + `getFilterFacets()`
- `providers.dart` — `filterFacetsProvider`(`FutureProvider.autoDispose` — 시트를 닫으면 버려져 다음에 열 때 선택지가 낡지 않는다)

---

## 19. 구현 상태 요약 ★

### ✅ 구현·실기 검증 완료

- 데이터 계층: Drift 12테이블(**schemaVersion 3**) + Supabase 4마이그레이션(RLS·트리거·FK cascade)
- 도메인 모델(애그리게이트·상속·title·세 표현 경계)
- 리포지토리: write·flush·read·sync·reconcile·profile
- 백엔드 실가동(익명 로그인·RLS·정본 프로젝트)
- 등록 폼 전체(신규·편집·악장·제목·override)
- 코어 화면: 목록·상세·홈
- **위시리스트 UI (v06)**: 목록·추가(자유 텍스트)·삭제, wishlist 스키마 확장 — 실기 검증 완료
- 회귀 테스트 2개(`save_album_replace_children_test.dart`, `album_filter_test.dart`)
- `flutter analyze` 에러 0 (info 2건, 이번 변경과 무관)

### 🔨 구현 완료 · 실기 탭 검증 미확인

- **필터 시트 UI (v06)**: 5축 필터, facet, 조합, 검색어 동시 적용.
  검증된 것 — 헤드리스 5건 통과, `flutter analyze` 에러 0, 실기 실행 시 예외·크래시 없음(로그 211줄에 EXCEPTION/error/overflow/assert 0건).
  **미확인** — 화면에서 실제로 의도대로 걸러지는지(드롭다운 값 표시·조합 결과·뱃지 숫자·초기화 후 검색어 잔존·취소 무반영). 다음 세션에서 확인해 위 ✅ 항목으로 옮길 것.

### ❌ 미구현

| 항목 | 예정 |
|---|---|
| 표 전환·세트 일괄·악장 순서 이동 | 대 1-B 잔여 / 대 2 |
| 위시 → album/work 확정 연결 | 대 1-A 이후 (§17-20) |
| 위시리스트 자동 해소 감지 | 미정 (§17-21) |
| FTS5 trigram | 별도 작업 (§17-23) |
| Works 시드 적재 | 대 1-A |
| 검증 큐·보류함·CSV | 대 1-G/1-H |
| 자동입력(바코드·OCR·검색·커버) | 대 2 |
| AI 해설·Edge Function·서버측 한도 | 대 3 |
| 통계(클래식) | 대 5 |
| 익명→정식 계정, 계정 삭제 | 대 7 |

### ⚠️ 검증 부채 / 배선 주의

- 오프라인 큐 왕복 미검증
- 재설치 복원 미검증
- watch 갱신: 하위 직접 UPDATE 경로 생기면 albums touch 없어 목록 미갱신
- LWW 통째 덮어쓰기: Album 편집 시 폼 미노출 필드 pre-fill 보존 필수
- **필터 시트 실기 탭 검증 미확인** — 헤드리스·정적 검증만 통과(위 🔨 항목)

---

## 20. Git 커밋 이력

이전 세션(v04 이전) 이력은 v05 §20 참조. **이번 세션 추가분** (해시는 push 후 `git log`로 확인해 채울 것):

```
feat(db) wishlist 자유 텍스트 컬럼 + CHECK 완화
feat(v04) 위시리스트 UI (1-D)
feat(v05) 필터 시트 UI (1-E)              ← test/album_filter_test.dart 포함
docs: v04→v06 전환, 1-D/1-E 반영           ← 이 문서 커밋 (예정)
```

커밋 메시지는 PowerShell `-F` 파일(utf8NoBOM)로 작성. `git add` 명시.

---

## 21. 변경 이력

**v06 (본 문서)** — `아키텍처_v05.md` 대체(저장소엔 처음 커밋되는 버전, v05는 리포에 없었음).
- 위시리스트 UI 구현(목록·추가·삭제) + 스키마 확장(composer/title, CHECK 완화, §3-1b)
- Drift schemaVersion 2→3(onUpgrade, 무손실 검증)
- 필터 시트 UI 구현(5축, facet, 조합, 검색어 동시 적용) + 회귀 테스트
- §16 1-D·1-E ✅ 전환, §17 부채 20~23 추가(위시 확정연결·자동해소·period 필터 미문서화·FTS5), §18/§19 갱신

**v05** — 코어 컬렉션 루프 실데이터 완성(백엔드 실가동·sync 경로·등록 폼 전체·title 필드·profiles).
**v04** — 데이터 계층·코어 화면 구현.
**v03** — Works 3단 하이브리드, 원어 입력 검색, WorkMovements 분리.
**v02** — Works 엔티티, 작품 단위 희망, 미결정 6건 확정.
**v1** — Isar→Drift, 커뮤니티·공연 제외, 연주자 Composition 귀속, AlbumDraft 수렴, 신뢰도, 보안 명문화.
